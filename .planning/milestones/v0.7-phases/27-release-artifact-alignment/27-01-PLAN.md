---
phase: 27-release-artifact-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/sign-notarize.yml
  - scripts/verify-app-build.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - SIGN-03
must_haves:
  truths:
    - "`sign-notarize.yml` builds the menu bar app with `xcodebuild -configuration Release`."
    - "The workflow writes `APM44 Bridge.app` to `build/Release`."
    - "The workflow signs and verifies the same Release app path."
  artifacts:
    - path: .github/workflows/sign-notarize.yml
      provides: "Release app artifact alignment"
      contains: "CONFIGURATION_BUILD_DIR=\"$PWD/build/Release\""
    - path: scripts/verify-app-build.sh
      provides: "App build proof with explicit output directory"
      contains: "APM44_APP_OUTPUT_DIR"
---

<objective>
Align the manual signing workflow with the exact Release app artifact path consumed by signing and verification scripts.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/27-release-artifact-alignment/27-CONTEXT.md
@.github/workflows/sign-notarize.yml
@scripts/verify-app-build.sh
@scripts/sign-release.sh
@scripts/codesign-verify-release.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Release app at the signing path</name>
  <files>.github/workflows/sign-notarize.yml, scripts/verify-app-build.sh</files>
  <acceptance_criteria>
    - The workflow invokes `xcodebuild -configuration Release`.
    - The app output directory is `build/Release`.
    - `scripts/verify-app-build.sh` can prove the executable exists at that explicit output path.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Regression-gate workflow path alignment</name>
  <files>tests/test_release_scripts.sh</files>
  <acceptance_criteria>
    - Release-script tests fail if the workflow no longer builds Release or no longer uses `build/Release`.
    - Release-script tests fail if the signing step no longer exports the same `APM44_APP_PATH`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
bash scripts/ci.sh
```
</verification>
