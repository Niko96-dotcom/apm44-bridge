---
phase: 27-release-artifact-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
must_haves:
  truths:
    - "`scripts/ci.sh` names the exact app bundle path under test."
    - "CI embeds the current daemon into that exact app bundle."
    - "Installed-sync dry-run fails if the checked helper is missing."
  artifacts:
    - path: scripts/ci.sh
      provides: "Concrete app path for embed and installed-sync"
      contains: "APP_PATH=\"$BUILD_DIR/$CONFIG/APM44 Bridge.app\""
    - path: tests/test_release_scripts.sh
      provides: "CI app-bundle proof source guard"
      contains: "run_ci_03_local_ci_app_bundle_proof_check"
---

<objective>
Make local CI prove the app bundle it embeds and verifies, instead of allowing an implicit or stale app path.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/27-release-artifact-alignment/27-CONTEXT.md
@scripts/ci.sh
@scripts/embed-daemon-in-app.sh
@scripts/verify-installed-sync.sh
@tests/test_release_scripts.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the app path through CI bundle checks</name>
  <files>scripts/ci.sh</files>
  <acceptance_criteria>
    - CI sets a concrete `APP_PATH`.
    - CI builds the app into that path when Swift app verification is active.
    - CI passes that path to embed and installed-sync checks.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fail on missing app bundle or helper</name>
  <files>scripts/ci.sh, tests/test_release_scripts.sh</files>
  <acceptance_criteria>
    - CI exits nonzero if the expected app bundle is missing after app build.
    - CI exits nonzero if the embedded helper is missing after embed.
    - Source regression checks cover the explicit app path and helper guard.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
bash scripts/ci.sh
```
</verification>
