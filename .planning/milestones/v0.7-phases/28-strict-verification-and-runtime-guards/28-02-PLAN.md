---
phase: 28-strict-verification-and-runtime-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - BridgeDaemon/src/engine/MetricsPublisher.h
  - App/APM44Bridge/BridgeProcessManager.swift
  - tests/test_bridge_metrics_json.cpp
  - tests/test_bridge_process_manager.swift
autonomous: true
requirements:
  - METR-01
  - APP-01
must_haves:
  truths:
    - "Packed metric atomics have a compile-time lock-free assertion."
    - "Clean running-process termination uses `transitionToIdle()`."
    - "Native and Swift regressions cover both guardrails."
  artifacts:
    - path: BridgeDaemon/src/engine/MetricsPublisher.h
      provides: "Lock-free atomic storage assertion"
      contains: "std::atomic<uint64_t>::is_always_lock_free"
    - path: App/APM44Bridge/BridgeProcessManager.swift
      provides: "Clean termination central idle transition"
      contains: "transitionToIdle()"
---

<objective>
Add the remaining metrics and app lifecycle guardrails required by v0.7.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/28-strict-verification-and-runtime-guards/28-CONTEXT.md
@BridgeDaemon/src/engine/MetricsPublisher.h
@App/APM44Bridge/BridgeProcessManager.swift
@tests/test_bridge_metrics_json.cpp
@tests/test_bridge_process_manager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert packed atomics are lock-free</name>
  <files>BridgeDaemon/src/engine/MetricsPublisher.h, tests/test_bridge_metrics_json.cpp</files>
  <acceptance_criteria>
    - The compile-time assertion is adjacent to the metrics publisher storage.
    - Native tests/source guards assert the lock-free assertion remains present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Route clean running termination through idle transition</name>
  <files>App/APM44Bridge/BridgeProcessManager.swift, tests/test_bridge_process_manager.swift</files>
  <acceptance_criteria>
    - Clean termination from `.running` invokes the central idle transition.
    - Swift regression proves metrics state and stale timestamp are reset.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
ctest --test-dir build --output-on-failure
bash scripts/ci.sh
```
</verification>
