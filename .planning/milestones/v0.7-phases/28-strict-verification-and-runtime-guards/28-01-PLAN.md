---
phase: 28-strict-verification-and-runtime-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/codesign-verify-release.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Release codesign verification fails when Hardened Runtime is missing."
    - "Release codesign verification fails when Developer ID Application identity is missing."
    - "An explicit local-development override permits ad-hoc checks."
  artifacts:
    - path: scripts/codesign-verify-release.sh
      provides: "Strict release codesign posture"
      contains: "APM44_ALLOW_LOCAL_CODESIGN"
    - path: tests/test_release_scripts.sh
      provides: "Strict codesign fake regression tests"
      contains: "run_codesign_verify_case"
---

<objective>
Make release codesign verification strict by default, with only an explicit local-development override for weak signatures.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/28-strict-verification-and-runtime-guards/28-CONTEXT.md
@scripts/codesign-verify-release.sh
@tests/test_release_scripts.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail on missing runtime or Developer ID</name>
  <files>scripts/codesign-verify-release.sh</files>
  <acceptance_criteria>
    - Missing Hardened Runtime sets failure status.
    - Missing Developer ID Application identity sets failure status.
    - Existing deep/strict verification failure behavior remains unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add strictness and override regressions</name>
  <files>tests/test_release_scripts.sh</files>
  <acceptance_criteria>
    - Fake strict Developer ID + runtime case passes.
    - Fake no-runtime case fails without override.
    - Fake no-Developer-ID case fails without override.
    - Fake ad-hoc case passes only with `APM44_ALLOW_LOCAL_CODESIGN=1`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
bash scripts/ci.sh
```
</verification>
