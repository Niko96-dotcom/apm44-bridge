---
phase: 26-regression-and-release-safety-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tests/test_release_scripts.sh
  - scripts/verify-installed-sync.sh
autonomous: true
requirements:
  - CI-02
must_haves:
  truths:
    - "`.github/workflows/ci.yml` runs `bash tests/test_release_scripts.sh` after native tests."
    - "`tests/test_release_scripts.sh` fails if GitHub CI stops running release-script regressions."
    - "Full local CI passes."
    - "v0.6 traceability is complete."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Public CI release-script regression step"
      contains: "bash tests/test_release_scripts.sh"
    - path: tests/test_release_scripts.sh
      provides: "CI-02 workflow guard"
      contains: "run_ci_02_release_script_tests_in_github_ci"
    - path: scripts/verify-installed-sync.sh
      provides: "Bounded dry-run installed-sync verifier"
      contains: "capture_with_timeout"
---

<objective>
Wire release-script tests into GitHub CI and run v0.6 validation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/26-regression-and-release-safety-closure/26-CONTEXT.md
@.github/workflows/ci.yml
@tests/test_release_scripts.sh
@scripts/ci.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release-script regressions to GitHub CI</name>
  <files>.github/workflows/ci.yml, tests/test_release_scripts.sh, scripts/verify-installed-sync.sh</files>
  <acceptance_criteria>
    - CI has a "Release script tests" step after "Native tests".
    - The CI step runs `bash tests/test_release_scripts.sh`.
    - Release-script tests contain a CI-02 guard for the workflow step.
  </acceptance_criteria>
  <action>
    Add the GitHub workflow step and a release-script source/order guard.
  </action>
</task>

<task type="auto">
  <name>Task 2: Run final v0.6 validation and record closure evidence</name>
  <files>.planning/phases/26-regression-and-release-safety-closure/26-VERIFICATION.md</files>
  <acceptance_criteria>
    - `bash tests/test_release_scripts.sh` exits 0.
    - `bash scripts/ci.sh` exits 0.
    - Requirements traceability shows all 10 v0.6 requirements complete.
    - Installed-sync dry-run cannot hang indefinitely on embedded helper version checks.
  </acceptance_criteria>
  <action>
    Run the local release-script and CI gates. Record the exact evidence in Phase 26 verification.
  </action>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
bash scripts/ci.sh
```
</verification>
