---
phase: 14-release-automation-fail-closed
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .github/workflows/sign-notarize.yml
  - scripts/ci.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - REL-06
  - REL-07
must_haves:
  truths:
    - "sign-notarize workflow does not mask app build verification failure"
    - "Credential-free release-script tests cover accepted, rejected, auth-failure, network-failure, and malformed notary output"
    - "Credential-free tests cover release-all missing credentials and explicit unnotarized override"
    - "scripts/ci.sh runs release-script regression tests without Apple credentials"
  artifacts:
    - path: .github/workflows/sign-notarize.yml
      provides: "Strict app build verification in signing workflow"
      contains: "bash scripts/verify-app-build.sh"
    - path: tests/test_release_scripts.sh
      provides: "Mocked xcrun/security regression matrix"
      contains: "malformed"
    - path: scripts/ci.sh
      provides: "CI wiring for credential-free release-script regressions"
      contains: "tests/test_release_scripts.sh"
  key_links:
    - from: tests/test_release_scripts.sh
      to: scripts/notarize-release-dmg.sh
      via: "fake xcrun notarytool matrix"
      pattern: "notarize-release-dmg.sh"
    - from: scripts/ci.sh
      to: tests/test_release_scripts.sh
      via: "release-script regression gate"
      pattern: "test_release_scripts.sh"
---

<objective>
Tighten signing workflow verification and add credential-free mocked release
script regression tests.

Purpose: Close REL-06 and REL-07 after strict release script behavior exists.
Output: Workflow fix, shell test matrix, and CI wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/14-release-automation-fail-closed/14-CONTEXT.md
@.planning/phases/14-release-automation-fail-closed/14-01-PLAN.md
@.github/workflows/sign-notarize.yml
@scripts/ci.sh
@scripts/notarize-release-dmg.sh
@scripts/notarize-release-pkg.sh
@scripts/release-all.sh

<interfaces>
Credential-free tests must not require Apple Developer credentials,
Developer ID certificates, a keychain notary profile, or a live network call to
Apple. They should run as:
```bash
bash tests/test_release_scripts.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop masking app build verification in signing workflow</name>
  <files>.github/workflows/sign-notarize.yml</files>
  <read_first>
    - .github/workflows/sign-notarize.yml
  </read_first>
  <acceptance_criteria>
    - `grep -n 'verify-app-build.sh || true' .github/workflows/sign-notarize.yml` returns no matches
    - `grep -n 'bash scripts/verify-app-build.sh' .github/workflows/sign-notarize.yml` returns at least one match
  </acceptance_criteria>
  <action>
    Remove the `|| true` from the app build verification step. Leave
    credential-dependent signing and notarization skips unchanged unless they
    mask build verification itself.
  </action>
  <verify>
    <automated>grep -n 'verify-app-build.sh || true' .github/workflows/sign-notarize.yml && exit 1 || true</automated>
  </verify>
  <done>Build verification failures stop the signing workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Add mocked release-script test matrix</name>
  <files>tests/test_release_scripts.sh</files>
  <read_first>
    - scripts/notarize-release-dmg.sh
    - scripts/notarize-release-pkg.sh
    - scripts/release-all.sh
    - scripts/notary-result.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'accepted' tests/test_release_scripts.sh` returns at least one match
    - `grep -n 'rejected' tests/test_release_scripts.sh` returns at least one match
    - `grep -n 'auth-failure' tests/test_release_scripts.sh` returns at least one match
    - `grep -n 'network-failure' tests/test_release_scripts.sh` returns at least one match
    - `grep -n 'malformed' tests/test_release_scripts.sh` returns at least one match
    - `bash tests/test_release_scripts.sh` exits 0
  </acceptance_criteria>
  <action>
    Create a shell test runner that builds temporary fake `xcrun` and
    `security` commands on `PATH`. The fake `xcrun` must simulate:
    accepted, rejected, auth-failure, network-failure, and malformed
    `notarytool submit` output. It must also record whether `stapler` and
    `notarytool log` were called.

    Cover both DMG and PKG notary scripts where practical, and cover
    `release-all.sh` missing credentials plus `APM44_ALLOW_UNNOTARIZED=1`.
  </action>
  <verify>
    <automated>bash tests/test_release_scripts.sh</automated>
  </verify>
  <done>Release script failure modes are covered without Apple credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Wire release-script regressions into CI script</name>
  <files>scripts/ci.sh</files>
  <read_first>
    - scripts/ci.sh
    - tests/test_release_scripts.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'test_release_scripts.sh' scripts/ci.sh` returns at least one match
    - `bash tests/test_release_scripts.sh` exits 0
  </acceptance_criteria>
  <action>
    Add a `Release script tests` step to `scripts/ci.sh` after native tests.
    The step must not require Apple credentials and should run before optional
    Swift app validation.
  </action>
  <verify>
    <automated>bash tests/test_release_scripts.sh</automated>
  </verify>
  <done>Local and CI verification run the release-script safety matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions release workflow | Workflow runs near signing secrets and release artifacts |
| Mocked release script tests | Fake tools stand in for Apple CLI behavior without credentials |
| Local CI script | Maintainer and CI machines depend on one gate command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | R | sign-notarize workflow | mitigate | Remove masked app build verification failure |
| T-14-02-02 | T | release script regression matrix | mitigate | Fake xcrun covers Accepted, rejected, auth, network, malformed outputs |
| T-14-02-03 | I | ci.sh | mitigate | Run credential-free release-script tests in the normal gate |
</threat_model>

<verification>
```bash
bash tests/test_release_scripts.sh
bash scripts/ci.sh
```
</verification>

<success_criteria>
- REL-06 and REL-07 are implemented and regression-tested.
- `scripts/ci.sh` runs release script tests without Apple credentials.
- The signing workflow no longer masks app build verification failures.
</success_criteria>

<output>
After completion, create `.planning/phases/14-release-automation-fail-closed/14-02-SUMMARY.md`
</output>
