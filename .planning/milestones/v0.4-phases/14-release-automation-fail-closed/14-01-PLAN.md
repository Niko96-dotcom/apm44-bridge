---
phase: 14-release-automation-fail-closed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/notary-result.sh
  - scripts/notarize-release-dmg.sh
  - scripts/notarize-release-pkg.sh
  - scripts/release-all.sh
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - REL-05
must_haves:
  truths:
    - "DMG notarization fails unless notarytool exits 0 and reports status Accepted"
    - "PKG notarization follows the same fail-closed contract as DMG notarization"
    - "Notary failure paths print submit output and attempt to fetch a log when an id is present"
    - "release-all.sh fails on missing notary credentials unless APM44_ALLOW_UNNOTARIZED=1"
    - "Unnotarized override output is visibly labelled local-only and unnotarized"
  artifacts:
    - path: scripts/notary-result.sh
      provides: "Shared Bash helpers for strict notary submit parsing and failure log capture"
      contains: "require_notary_accepted"
    - path: scripts/notarize-release-dmg.sh
      provides: "Fail-closed DMG notarization"
      contains: "require_notary_accepted"
    - path: scripts/notarize-release-pkg.sh
      provides: "Fail-closed PKG notarization"
      contains: "require_notary_accepted"
    - path: scripts/release-all.sh
      provides: "Missing-credential hard failure and explicit local-only override"
      contains: "APM44_ALLOW_UNNOTARIZED"
  key_links:
    - from: scripts/release-all.sh
      to: scripts/notarize-release-dmg.sh
      via: "release-all final DMG notarization"
      pattern: "notarize-release-dmg.sh"
    - from: scripts/notarize-release-dmg.sh
      to: scripts/notary-result.sh
      via: "strict status parsing"
      pattern: "require_notary_accepted"
---

<objective>
Make release notarization and the primary release command fail closed by default.

Purpose: Close REL-01 through REL-05 before release workflow and validation
coverage are trusted.
Output: Strict notary result parsing, log capture on failure, and a local-only
unnotarized override path that cannot look publishable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/14-release-automation-fail-closed/14-CONTEXT.md
@scripts/notarize-release-dmg.sh
@scripts/notarize-release-pkg.sh
@scripts/release-all.sh

<interfaces>
Existing public entrypoints remain stable:
```bash
bash scripts/notarize-release-dmg.sh
bash scripts/notarize-release-pkg.sh
bash scripts/release-all.sh
```

Environment variables:
- `NOTARY_PROFILE` selects the keychain profile, default `AC_NOTARY`.
- `APM44_DMG_PATH` and `APM44_PKG_PATH` may point at specific artifacts.
- `APM44_BUILD_PKG=1` opts into the optional PKG build/notarization path.
- `APM44_ALLOW_UNNOTARIZED=1` is the only local-only release-all override.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared strict notary result helper</name>
  <files>scripts/notary-result.sh</files>
  <read_first>
    - scripts/notarize-release-dmg.sh
    - scripts/notarize-release-pkg.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'require_notary_accepted' scripts/notary-result.sh` returns at least one match
    - `grep -n 'status:[[:space:]]*Accepted' scripts/notary-result.sh` returns at least one match
    - `grep -n 'notarytool log' scripts/notary-result.sh` returns at least one match
  </acceptance_criteria>
  <action>
    Create a Bash helper that captures notary submit output, preserves the real
    `notarytool` exit status, prints the submit output, requires parsed
    `status: Accepted`, and fetches a failure log when a submission id is
    present. Keep the helper dependency-free and sourceable from existing
    scripts.
  </action>
  <verify>
    <automated>bash -n scripts/notary-result.sh</automated>
  </verify>
  <done>Notary status parsing is centralized and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fail-closed notary contract to DMG and PKG scripts</name>
  <files>scripts/notarize-release-dmg.sh, scripts/notarize-release-pkg.sh</files>
  <read_first>
    - scripts/notary-result.sh
    - scripts/notarize-release-dmg.sh
    - scripts/notarize-release-pkg.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n '|| true' scripts/notarize-release-dmg.sh scripts/notarize-release-pkg.sh` returns no matches
    - `grep -n 'status: Invalid' scripts/notarize-release-dmg.sh scripts/notarize-release-pkg.sh` returns no matches
    - `grep -n 'require_notary_accepted' scripts/notarize-release-dmg.sh scripts/notarize-release-pkg.sh` returns two matches
  </acceptance_criteria>
  <action>
    Replace each `RESULT=$(xcrun notarytool submit ... 2>&1) || true` flow with
    the shared helper. Stapling must run only after the helper confirms a
    successful submit exit status and `status: Accepted`.
  </action>
  <verify>
    <automated>bash -n scripts/notarize-release-dmg.sh scripts/notarize-release-pkg.sh</automated>
  </verify>
  <done>DMG and PKG scripts fail closed on all non-accepted notary results.</done>
</task>

<task type="auto">
  <name>Task 3: Make release-all require notary credentials by default</name>
  <files>scripts/release-all.sh</files>
  <read_first>
    - scripts/release-all.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'APM44_ALLOW_UNNOTARIZED' scripts/release-all.sh` returns at least one match
    - `grep -n 'local-only unnotarized' scripts/release-all.sh` returns at least one match
    - `grep -n 'exit 1' scripts/release-all.sh` returns at least one missing-credential failure path
  </acceptance_criteria>
  <action>
    Keep the notary credential probe, but invert the missing-profile behavior:
    fail with clear instructions unless `APM44_ALLOW_UNNOTARIZED=1` is set.
    When the override is set, print loud local-only unnotarized labels and list
    artifacts under that warning instead of as normal release artifacts.
  </action>
  <verify>
    <automated>bash -n scripts/release-all.sh</automated>
  </verify>
  <done>release-all cannot silently produce public-looking unnotarized artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple notary service | Local scripts interpret external CLI output and exit statuses |
| Public release artifact directory | Files under `build/signing` may be uploaded to GitHub releases |
| Maintainer credential state | Release commands run on machines with or without notary profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | T | notarize-release-dmg.sh | mitigate | Require submit exit 0 and `status: Accepted` before stapling |
| T-14-01-02 | T | notarize-release-pkg.sh | mitigate | Share the same fail-closed notary helper as DMG |
| T-14-01-03 | R | notary failure handling | mitigate | Preserve submit output and fetch logs when id is available |
| T-14-01-04 | I | release-all.sh | mitigate | Fail on missing credentials unless explicit local-only override is set |
</threat_model>

<verification>
```bash
bash -n scripts/notary-result.sh scripts/notarize-release-dmg.sh scripts/notarize-release-pkg.sh scripts/release-all.sh
```
</verification>

<success_criteria>
- REL-01 through REL-05 are implemented.
- DMG and PKG notarization cannot pass on rejected, auth-failed, network-failed,
  or malformed output.
- Missing notary credentials stop `release-all.sh` unless the local-only
  override is explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/14-release-automation-fail-closed/14-01-SUMMARY.md`
</output>
