---
phase: 36-release-automation-and-workflow-intent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release-all.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "`scripts/release-all.sh` runs `bash scripts/codesign-verify-release.sh` before `bash scripts/notary-dry-run.sh`."
    - "Credential-free release-script tests fail if the strict codesign gate is omitted or ordered after notarization."
    - "`APM44_ALLOW_UNNOTARIZED=1` remains local-only and does not submit to notary service."
---

<objective>
Insert strict release codesign verification into `release-all.sh` before notarization and regression-test the order.
</objective>

<tasks>

<task type="auto">
  <name>Add strict pre-notary release gate</name>
  <read_first>scripts/release-all.sh, tests/test_release_scripts.sh</read_first>
  <action>Add a `== Verify release codesigning ==` step in the notary-ready branch of `scripts/release-all.sh`, before `bash scripts/notary-dry-run.sh`, that runs `bash scripts/codesign-verify-release.sh`. Extend the fake bash test harness to log and succeed for `scripts/codesign-verify-release.sh`. Assert the codesign verification log line exists and occurs before the notary dry-run line.</action>
  <acceptance_criteria>
    - `scripts/release-all.sh` contains `bash scripts/codesign-verify-release.sh`.
    - `tests/test_release_scripts.sh` contains `codesign_verify_line`.
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
