---
phase: 35-public-version-and-defaults-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/install.md
  - docs/menu-bar-qa.md
  - docs/release.md
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "Install docs and menu-bar QA identify Safe as the fresh-install default."
    - "Release docs use `build/Driver/APM44Bridge.driver`, not `build/Release/APM44Bridge.driver`."
    - "Release-script tests guard against stale Balanced-default and HAL driver path wording."
---

<objective>
Align latency-default wording and HAL driver build-path references with current code/scripts.
</objective>

<tasks>

<task type="auto">
  <name>Patch defaults/path docs and source guards</name>
  <read_first>docs/install.md, docs/menu-bar-qa.md, docs/release.md, tests/test_release_scripts.sh</read_first>
  <action>Change user-facing latency docs and QA checklist to identify Safe as the fresh-install default. Replace all `build/Release/APM44Bridge.driver` references in `docs/release.md` with `build/Driver/APM44Bridge.driver`. Extend `run_doc_truth_check` to fail if stale Balanced-default wording or the wrong HAL driver build path returns.</action>
  <acceptance_criteria>
    - `docs/install.md` contains `fresh-install default`.
    - `docs/menu-bar-qa.md` contains `Safe default on fresh install`.
    - `docs/release.md` does not contain `build/Release/APM44Bridge.driver`.
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
