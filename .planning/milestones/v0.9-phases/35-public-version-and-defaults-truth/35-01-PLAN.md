---
phase: 35-public-version-and-defaults-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release-validation.md
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "Release validation identifies v0.9 public-polish validation, not stale v0.8 closeout."
    - "0.1.1 remains the current public DMG artifact identity."
    - "Release-script tests guard against stale release-validation version wording."
---

<objective>
Normalize release-validation version identity while preserving the current 0.1.1 public DMG artifact commands.
</objective>

<tasks>

<task type="auto">
  <name>Update release-validation identity and guard it</name>
  <read_first>docs/release-validation.md, tests/test_release_scripts.sh</read_first>
  <action>Change the release-validation introduction to describe the v0.9 public-polish validation path for the current 0.1.1 DMG-primary distribution flow. Add a release-script source guard that asserts `docs/release-validation.md` no longer contains `v0.8 release-candidate closeout` and still contains `APM44Bridge-${APM44_VERSION:-0.1.1}.dmg`.</action>
  <acceptance_criteria>
    - `docs/release-validation.md` contains `v0.9 public-polish validation path`.
    - `tests/test_release_scripts.sh` contains `run_doc_truth_check`.
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
