---
phase: 34-shared-memory-compatibility-truth
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - tests/test_mmap_shm_validation.cpp
  - docs/hal-driver.md
  - docs/release.md
autonomous: true
requirements:
  - SHM-01
  - SHM-02
  - SHM-03
must_haves:
  truths:
    - "Regression tests reject mismatched shm sample rates."
    - "Regression tests reject mismatched producer build IDs."
    - "Public docs identify build ID and sample rate as hard validation gates."
---

<objective>
Add regression and documentation guards for the selected shared-memory compatibility contract.
</objective>

<tasks>

<task type="auto">
  <name>Add shm compatibility regressions and doc truth</name>
  <read_first>tests/test_mmap_shm_validation.cpp, docs/hal-driver.md, docs/release.md</read_first>
  <action>Add isolated shm validation tests for a syntactically valid header with `sample_rate = 48000` and for a syntactically valid header with `producer_build_id = "stale-build"`, both rejected by `MmapShmRing::open`. Update docs to state that ABI version, sample rate, channels, object size/capacity, and producer build ID are hard validation gates, while generation/object identity support stale mapping diagnostics.</action>
  <acceptance_criteria>
    - `tests/test_mmap_shm_validation.cpp` contains `OpenRejectsMismatchedSampleRate`.
    - `tests/test_mmap_shm_validation.cpp` contains `OpenRejectsMismatchedProducerBuildId`.
    - `docs/hal-driver.md` contains `Hard validation gates`.
    - `ctest --test-dir build --output-on-failure -R test_mmap_shm_validation` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_mmap_shm_validation
ctest --test-dir build --output-on-failure -R test_mmap_shm_validation
```
</verification>
