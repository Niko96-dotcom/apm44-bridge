---
phase: 34-shared-memory-compatibility-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/include/apm44/ShmRingLayout.h
  - Shared/src/MmapShmRing.cpp
autonomous: true
requirements:
  - SHM-01
  - SHM-02
  - SHM-03
must_haves:
  truths:
    - "`ValidateShmHeader` rejects rings whose `sample_rate` is not `44100`."
    - "`ValidateShmHeader` rejects rings whose `producer_build_id` differs from the consumer `kBuildId`."
    - "Header mismatch diagnostics include actual and expected sample-rate/build-ID evidence."
---

<objective>
Make shared-memory build ID and sample rate hard compatibility gates during `MmapShmRing::open`.
</objective>

<tasks>

<task type="auto">
  <name>Enforce shm sample-rate and build-ID compatibility</name>
  <read_first>Shared/include/apm44/ShmRingLayout.h, Shared/src/MmapShmRing.cpp</read_first>
  <action>Add a shared `kShmSampleRate = 44100` constant. Use it when creating ring headers and when validating headers. Extend `ValidateShmHeader` so it returns false when `header.sample_rate != kShmSampleRate` or the bounded rendered `producer_build_id` differs from the bounded rendered local `kBuildId`. Extend `DescribeHeaderMismatch` to include `sample_rate`, `expected_sample_rate`, and `expected_consumer_build_id`.</action>
  <acceptance_criteria>
    - `Shared/include/apm44/ShmRingLayout.h` contains `kShmSampleRate`.
    - `Shared/include/apm44/ShmRingLayout.h` contains `header.sample_rate != kShmSampleRate`.
    - `Shared/include/apm44/ShmRingLayout.h` contains `RenderShmBuildId(header.producer_build_id) != RenderShmBuildId(kBuildId)`.
    - `Shared/src/MmapShmRing.cpp` contains `expected_sample_rate=`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_mmap_shm_validation
ctest --test-dir build --output-on-failure -R test_mmap_shm_validation
```
</verification>
