---
phase: 33-asbd-memory-contract
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - tests/test_audio_formats.cpp
autonomous: true
requirements:
  - ASBD-04
must_haves:
  truths:
    - "Regression tests reject interleaved Float32 stereo with one-float byte sizes."
    - "Regression tests reject non-interleaved Float32 stereo with two-float byte sizes."
    - "Regression tests reject otherwise valid Float32 stereo when `mFramesPerPacket != 1`."
---

<objective>
Add ASBD layout regression tests for odd Float32 stereo variants so IOProc memory assumptions remain guarded.
</objective>

<tasks>

<task type="auto">
  <name>Add byte-layout rejection tests</name>
  <read_first>tests/test_audio_formats.cpp, Shared/src/AudioFormats.cpp</read_first>
  <action>Add Catch2 regression cases proving `AsbdMatchesFloat32Stereo` accepts the current valid interleaved and non-interleaved layouts while rejecting wrong byte-size fields, missing packed interleaved ASBDs, and `mFramesPerPacket != 1`.</action>
  <acceptance_criteria>
    - `tests/test_audio_formats.cpp` contains `rejects unsupported packet and byte layouts`.
    - `tests/test_audio_formats.cpp` contains `mFramesPerPacket = 2`.
    - `ctest --test-dir build --output-on-failure -R test_audio_formats` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_audio_formats
ctest --test-dir build --output-on-failure -R test_audio_formats
```
</verification>
