---
phase: 04-hal-virtual-device
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/include/apm44/ShmRingLayout.h
  - Shared/include/apm44/MmapShmRing.h
  - Shared/src/MmapShmRing.cpp
  - Shared/CMakeLists.txt
  - tests/test_mmap_shm_ring.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - DRV-03
must_haves:
  truths:
    - "Documented shm header + planar sample region layout shared by driver and daemon"
    - "Daemon can create/open shm segment and push/pop without HAL"
  artifacts:
    - path: Shared/include/apm44/ShmRingLayout.h
      provides: "Wire format v1"
    - path: Shared/include/apm44/MmapShmRing.h
      provides: "Producer/consumer mmap API"
  key_links:
    - from: MmapShmRing.cpp
      to: ShmRingLayout.h
      pattern: "kShmMagic"
---

<objective>
Implement cross-process **mmap SPSC ring** transport in `Shared/` per 04-RESEARCH.md (DRV-03).
</objective>

<context>
@.planning/phases/04-hal-virtual-device/04-RESEARCH.md
@Shared/include/apm44/PlanarRingBuffer.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShmRingLayout header</name>
  <files>Shared/include/apm44/ShmRingLayout.h</files>
  <action>
Define constants: `kShmName`, `kShmMagic`, `kShmVersion`, `kDefaultCapacityFrames` (4096 power-of-two).

`struct ShmRingHeader` with atomics for write/read indices, capacity, generation, daemon_ready flag.

Provide `shmTotalSize(capacity)`, `shmSamplesOffset()`, `validateHeader()`.

RT note: header fields written once at create; indices updated with release/acquire ordering.
  </action>
  <verify>
    <automated>rg -n 'kShmMagic' Shared/include/apm44/ShmRingLayout.h</automated>
  </verify>
  <done>Header compiles; magic/version documented in file comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MmapShmRing + unit test</name>
  <files>Shared/include/apm44/MmapShmRing.h, Shared/src/MmapShmRing.cpp, tests/test_mmap_shm_ring.cpp, tests/CMakeLists.txt, Shared/CMakeLists.txt</files>
  <behavior>
    - `createOrOpen` maps segment; producer pushes 100 frames sine; consumer pops ≥95.
    - Overrun: push beyond capacity drops without corrupting indices.
  </behavior>
  <action>
`MmapShmRing` roles: `Producer` (driver) and `Consumer` (daemon).

Methods: `create()`, `open()`, `close()`, `push(planar)`, `pop(planar)` — no malloc after map.

Add Catch2 test using two processes OR fork in test (prefer single-process producer+consumer roles sequentially if fork fragile in CI).

Link test to `apm44_shared`.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R mmap_shm_ring --output-on-failure</automated>
  </verify>
  <done>CTest passes; Shared library exports MmapShmRing.</done>
</task>

</tasks>

<verification>
- `ctest -R mmap_shm_ring` passes on macOS
</verification>

<success_criteria>
- Driver and daemon can share documented layout without duplicating struct definitions
</success_criteria>
