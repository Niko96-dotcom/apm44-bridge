---
phase: 05-integration-ship-readiness
plan: 01
type: execute
wave: 1
depends_on:
  - "03-07"
autonomous: true
requirements:
  - QA-02
must_haves:
  truths:
    - "DAW matrix documents Logic + Ableton validation with pass/fail columns"
    - "Export bounce stays 44.1 kHz when project rate is 44.1"
    - "Release signing documented with Developer ID + notarytool dry-run"
  artifacts:
    - path: docs/daw-matrix.md
      provides: "DAW validation checklist"
    - path: scripts/validate-export-rate.sh
      provides: "Export rate verification helper"
    - path: docs/release.md
      provides: "codesign + notarytool distribution guide"
    - path: Driver/APM44Bridge.entitlements
      provides: "HAL driver entitlements template"
---

## Phase Goal

**As a** producer shipping APM44 Bridge, **I want** documented DAW validation, export-rate verification, and signing/notarization steps, **so that** the full stack is integration-ready before distribution.

<objective>
Ship integration & ship-readiness artifacts: DAW matrix, export-rate script, release signing docs, entitlements plists, README refresh, phase verification report, and plan summaries.

Purpose: Close QA-02 documentation path and distribution readiness.
Output: docs/, scripts/, entitlements, README, 05-VERIFICATION.md, SUMMARYs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-integration-ship-readiness/05-CONTEXT.md
@docs/mvp-routing.md
@docs/soak-test.md
@docs/menu-bar-qa.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DAW matrix and export-rate validation script</name>
  <files>docs/daw-matrix.md, scripts/validate-export-rate.sh</files>
  <action>
Create `docs/daw-matrix.md`:
- Matrix table with pass/fail columns for Logic Pro and Ableton Live (minimum)
- Rows: project rate 44100, output device (BlackHole MVP + APM44 Bridge when Phase 4 lands), Audio MIDI Setup rates, monitoring audible, 30+ min stability ref to soak-test.md, export 44100 (QA-02)
- Separate MVP (BlackHole) vs production (APM44 Bridge) columns where paths differ

Create `scripts/validate-export-rate.sh`:
- `--help` documents bounce procedure for Logic and Ableton
- `--check-file PATH` uses `afinfo` to assert sample rate is 44100 Hz (exit 0/1)
- `--instructions` prints human steps only (no hardware required)
- Agent-friendly JSON mode optional via `--json` on check result
  </action>
  <verify>
    <automated>bash scripts/validate-export-rate.sh --instructions; bash scripts/validate-export-rate.sh --help</automated>
  </verify>
  <done>Matrix covers Logic + Ableton; script runs offline and documents bounce workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Release signing docs and entitlements</name>
  <files>docs/release.md, Driver/APM44Bridge.entitlements, scripts/install-driver.sh</files>
  <action>
Create `docs/release.md`:
- Developer ID Application signing for `apm44-bridge`, `APM44 Bridge.app`, `APM44Bridge.driver`
- Hardened runtime + timestamp flags per CLAUDE.md stack
- `notarytool submit` dry-run / staging workflow (zip/dmg, staple)
- Ad-hoc `-` signing for local HAL dev; macOS 15+ Developer ID requirement note
- Entitlements file references

Create `Driver/APM44Bridge.entitlements` (minimal plist for HAL plug-in; no app sandbox)

Create `scripts/install-driver.sh`:
- Requires built `APM44Bridge.driver` path arg or default build location
- Ad-hoc codesign for dev, sudo install to `/Library/Audio/Plug-Ins/HAL/`, kickstart coreaudiod
- Clear message when driver bundle missing (Phase 4 not built yet)
  </action>
  <verify>
    <automated>test -f docs/release.md && test -f Driver/APM44Bridge.entitlements && bash -n scripts/install-driver.sh</automated>
  </verify>
  <done>Release doc covers codesign + notarytool; entitlements and install script exist.</done>
</task>

<task type="auto">
  <name>Task 3: README, verification report, summaries</name>
  <files>README.md, .planning/phases/05-integration-ship-readiness/05-VERIFICATION.md, .planning/phases/05-integration-ship-readiness/05-01-SUMMARY.md</files>
  <action>
Update `README.md`:
- Reflect phases 1–3 shipped (daemon, drift SRC, menu bar app)
- Phase 4 HAL driver pending; link new docs (daw-matrix, release, soak-test, menu-bar)
- Build/test commands unchanged

Create `05-VERIFICATION.md`:
- Automated: cmake/ctest, validate-export-rate --instructions, verify-menu-bar if app builds
- Human-needed: DAW matrix rows, export bounce, 30+ min soak, signing dry-run on Apple Developer account

Create `05-01-SUMMARY.md` per template with task commits and self-check.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <done>README updated; verification report documents automated vs human gates.</done>
</task>

</tasks>

<verification>
- `cmake --build build && ctest --test-dir build --output-on-failure`
- `bash scripts/validate-export-rate.sh --instructions`
- All new docs present
</verification>

<success_criteria>
- QA-02 export validation path documented and scriptable
- DAW matrix ready for Logic + Ableton sign-off
- Release signing/notarization steps documented with entitlements
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-ship-readiness/05-01-SUMMARY.md`
</output>
