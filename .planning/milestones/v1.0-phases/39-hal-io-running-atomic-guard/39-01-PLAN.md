---
phase: 39
plan: 01
title: Make HAL IO running state atomic
status: complete
requirements: [HALIO-01, HALIO-02]
files_modified:
  - Driver/src/ShmIoHandler.h
  - Driver/src/ShmIoHandler.cpp
---

<objective>
Replace the plain `ioRunning_` bool with `std::atomic<bool>` and make all callback access use an explicit memory-ordering contract.
</objective>

<tasks>
1. Include `<atomic>` in `ShmIoHandler.h`.
2. Change `ioRunning_` to `std::atomic<bool>`.
3. Use release stores from start/stop and an acquire load in mixed-output processing.
</tasks>

<verification>
- Targeted: `cmake --build build --target apm44_driver_tests && ctest --test-dir build -R shm_io_handler --output-on-failure`
</verification>
