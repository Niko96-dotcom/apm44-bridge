---
phase: 39
plan: 02
title: Guard stopped-IO behavior against regressions
status: complete
requirements: [HALIO-03, HALIO-04]
files_modified:
  - tests/test_hardening_audit.cpp
  - tests/test_shm_io_handler.cpp
---

<objective>
Preserve the stopped-IO behavior and add source guards that fail if `ioRunning_` regresses to a plain bool.
</objective>

<tasks>
1. Keep the existing stopped-IO behavior test passing.
2. Add source-audit assertions for the atomic member and explicit memory-ordering calls.
</tasks>

<verification>
- Targeted: `ctest --test-dir build -R "shm_io_handler|hardening_audit" --output-on-failure`
</verification>
