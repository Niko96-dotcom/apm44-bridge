---
phase: 38
plan: 01
title: Remove producer-side drift mutation
status: complete
requirements: [DRIFT-01, DRIFT-02, DRIFT-03]
files_modified:
  - BridgeDaemon/src/engine/BridgeInputOverrun.h
  - BridgeDaemon/src/engine/BridgeEngine.cpp
  - BridgeDaemon/src/engine/BridgeEngine.h
  - tests/test_planar_ring_buffer.cpp
  - tests/test_hardening_audit.cpp
---

<objective>
Make input overrun handling return an overrun flag without depending on `DriftController`; `BridgeEngine::onInput` records producer drops in an atomic counter.
</objective>

<tasks>
1. Change `PushDroppingNewInput` to return `bool` and remove the `DriftController` parameter and include.
2. Add/reset `BridgeEngine::inputOverruns_` and increment it from `onInput` when the helper reports a partial push.
3. Update existing ring tests to assert the returned flag instead of drift-controller mutation.
4. Add/adjust source-audit guards for forbidden producer-side drift references.
</tasks>

<verification>
- Targeted: `cmake --build build --target apm44_tests && ctest --test-dir build -R "planar_ring|hardening_audit" --output-on-failure`
- Final phase gate: `scripts/ci.sh`
</verification>
