---
phase: 38
plan: 02
title: Publish input-overrun metrics from atomic counter
status: complete
requirements: [DRIFT-04, DRIFT-05]
files_modified:
  - BridgeDaemon/src/engine/BridgeEngine.cpp
  - BridgeDaemon/src/engine/BridgeEngine.h
  - tests/test_hardening_audit.cpp
---

<objective>
Ensure metrics snapshots publish input overruns from the dedicated atomic counter while drift underrun/ratio state stays output-owned.
</objective>

<tasks>
1. Read `inputOverruns_` with relaxed ordering during metrics snapshot publication.
2. Avoid all producer-side `DriftController` reads or writes.
3. Add source guards covering metrics ownership and the `BridgeInputOverrun` no-drift contract.
</tasks>

<verification>
- Targeted: `ctest --test-dir build -R hardening_audit --output-on-failure`
- Final phase gate: `scripts/ci.sh`
</verification>
