---
phase: 02-production-src-drift-engine
plan: 06
type: execute
wave: 4
depends_on:
  - 02-05
files_modified:
  - BridgeDaemon/src/tools/SoakHarness.cpp
  - BridgeDaemon/src/tools/SoakHarness.h
  - BridgeDaemon/CMakeLists.txt
  - tests/test_soak_offline.cpp
  - tests/CMakeLists.txt
  - docs/soak-test.md
  - scripts/ci-soak.sh
autonomous: false
requirements:
  - QA-01
must_haves:
  truths:
    - "Offline soak simulates clock skew between producer and consumer for N minutes (CI: 60s)"
    - "apm44-soak CLI runs harness with --duration-sec and reports max fill deviation, xruns"
    - "Human 30+ min soak documented with BlackHole + AirPods steps"
  artifacts:
    - path: BridgeDaemon/src/tools/SoakHarness.cpp
      provides: "Synthetic clock-skew soak"
    - path: docs/soak-test.md
      provides: "Manual QA-01 procedure"
    - path: scripts/ci-soak.sh
      provides: "CI shortened soak"
  key_links:
    - from: SoakHarness.cpp
      to: DriftController + PlanarRingBuffer + LibSamplerateSrc
      via: "offline loop"
      pattern: "DriftController"
---

## Phase Goal

**As a** producer monitoring through AirPods Max USB-C, **I want** the bridge to hold stable 44.1→48 conversion with drift correction over long sessions, **so that** I hear uninterrupted audio without crackle or growing latency.

<objective>
Validate **QA-01** with automated offline soak (`apm44-soak`, 60s in CI) and documented human 30+ minute procedure — without requiring DAW in CI.

Purpose: Primary phase success criterion is long-session stability; harness de-risks before human soak.
Output: Soak binary, offline test, `docs/soak-test.md`, CI script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-production-src-drift-engine/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-production-src-drift-engine/02-05-SUMMARY.md

<interfaces>
Soak harness loops:
- Synthetic input @ 44100 pushes sine with **simulated input clock** (e.g. +30 ppm every block).
- Output side consumes @ 48000 with `DriftController` + `LibSamplerateSrc` matching BridgeEngine logic (shared code paths, not duplicate PI).
- Assert: fill stays within ±50% of target for 95% of blocks; underrun count below threshold per minute; no unbounded fill growth (slope test).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline soak harness + apm44-soak binary</name>
  <files>BridgeDaemon/src/tools/SoakHarness.h, BridgeDaemon/src/tools/SoakHarness.cpp, BridgeDaemon/CMakeLists.txt, tests/test_soak_offline.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - 5-second offline soak at +50 ppm skew: underruns under 10, fill never exceeds 2× target for more than 1s wall time.
    - 60-second run completes exit 0 (CI profile).
  </behavior>
  <action>
1. Extract soak loop from BridgeEngine into `SoakHarness` (or free functions in `tools/`) callable from tests and CLI — **reuse** `PlanarRingBuffer`, `DriftController`, `LibSamplerateSrc` (no HAL).

2. Add executable `apm44-soak` with `--duration-sec` (default 60), `--skew-ppm` (default 50), `--target-fill-ms` (default 15).

3. Print summary: duration, mean fill ms, max fill ms, underruns, overruns, final ppm.

4. `test_soak_offline.cpp`: 5s fast test for CTest; tag `[soak]` for filtering.

5. `scripts/ci-soak.sh`: configure, build, run `ctest -R soak` and `apm44-soak --duration-sec 60`.

**Per CONTEXT soak validation:** synthetic sine + clock skew; CI 60s only.

**Forbidden:** requiring BlackHole in unit test.
  </action>
  <verify>
    <automated>cmake --build build; ctest --test-dir build -R soak --output-on-failure; ./build/BridgeDaemon/apm44-soak --duration-sec 5</automated>
  </verify>
  <done>Soak test and binary exit 0 on clean build; harness reports metrics JSON or key=value lines parseable by CI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human 30+ min soak documentation</name>
  <files>docs/soak-test.md</files>
  <action>
Write `docs/soak-test.md` per CONTEXT:

- Prerequisites: BlackHole 2ch @ 44100, AirPods USB-C @ 48000, `apm44-bridge` built.
- Steps: DAW or `afplay` loop → BlackHole; bridge command with recommended flags.
- Pass criteria: no crackle, no runaway latency (fill stable), xruns not steadily increasing.
- Record: date, macOS version, buffer sizes, `--target-fill-ms`, notes.
- Link to `scripts/ci-soak.sh` for automated subset.

Executor completes doc in repo; checkpoint confirms user can follow it (optional 30 min — user may approve after skim + 5 min smoke).
  </action>
  <what-built>docs/soak-test.md and working apm44-soak for quick validation</what-built>
  <how-to-verify>
    1. Read docs/soak-test.md end-to-end.
    2. Run `./build/BridgeDaemon/apm44-soak --duration-sec 30` (offline) — expect exit 0.
    3. Optional: run `apm44-bridge` with DAW 30+ min per doc; note fill/xrun stats on exit.
  </how-to-verify>
  <resume-signal>Type "approved" after doc review (and optional long soak), or describe issues</resume-signal>
  <verify>
    <automated>test -f docs/soak-test.md; rg -n '30' docs/soak-test.md | head -1</automated>
  </verify>
  <done>docs/soak-test.md documents QA-01 human soak; user approved checkpoint or filed issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script | Runs local binaries only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Repudiation | soak metrics | accept | Local QA logs only |
</threat_model>

<verification>
- `ctest -R soak` passes in CI script
- docs/soak-test.md exists and references QA-01 criteria
</verification>

<success_criteria>
- Automated soak proves drift+SRC stability under skew
- Human procedure documented for full QA-01 sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-src-drift-engine/02-06-SUMMARY.md`
</output>
