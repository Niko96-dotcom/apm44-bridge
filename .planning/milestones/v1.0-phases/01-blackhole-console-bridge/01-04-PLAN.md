---
phase: 01-blackhole-console-bridge
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - docs/mvp-routing.md
  - docs/blackhole-prerequisite.md
  - scripts/verify-devices.sh
  - README.md
  - BridgeDaemon/src/main.cpp
autonomous: false
requirements:
  - MVP-03
  - DEV-04
must_haves:
  truths:
    - "docs/mvp-routing.md documents DAW → BlackHole @ 44.1 → apm44-bridge → AirPods @ 48 with Logic and Ableton steps"
    - "User can follow checklist to confirm Audio MIDI Setup rates (44100 BlackHole, 48000 AirPods)"
    - "440 Hz DAW test tone is audible on AirPods with bridge running"
    - "BlackHole install is documented with GPL note and external link only"
  artifacts:
    - path: docs/mvp-routing.md
      provides: "Manual routing guide"
      min_lines: 80
    - path: scripts/verify-devices.sh
      provides: "Automated preflight"
      contains: "44100"
    - path: README.md
      provides: "Project entry documentation"
      contains: "apm44-bridge"
  key_links:
    - from: docs/mvp-routing.md
      to: scripts/verify-devices.sh
      via: "preflight step references script"
      pattern: "verify-devices"
    - from: README.md
      to: docs/mvp-routing.md
      via: "link to routing doc"
      pattern: "mvp-routing"
---

<objective>
Document the MVP routing workflow, finalize verification tooling, and validate the Phase 1 success demo (440 Hz tone) with a human checkpoint.

Purpose: Satisfy MVP-03 and ROADMAP success criteria #1–#4; make the bridge reproducible for Phase 2.
Output: Complete docs, polished README, enhanced verify script, demo checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-blackhole-console-bridge/01-CONTEXT.md
@.planning/phases/01-blackhole-console-bridge/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MVP routing docs + BlackHole prerequisite</name>
  <files>docs/mvp-routing.md, docs/blackhole-prerequisite.md, README.md</files>
  <action>
`docs/blackhole-prerequisite.md`:
- BlackHole 2ch v0.6.1+ install link (Existential Audio / GitHub releases).
- **GPL-3.0:** not bundled, not vendored, not statically linked — user installs separately.
- Audio MIDI Setup: set BlackHole 2ch nominal rate to **44100 Hz** (screenshot placeholders: `docs/images/blackhole-44100.png` optional — use `<!-- screenshot: ... -->` checklist if images not captured).

`docs/mvp-routing.md`:
- Architecture diagram (ASCII) matching PROJECT MVP routing.
- Step-by-step:
  1. Install BlackHole; verify with `scripts/verify-devices.sh`
  2. Set AirPods Max USB-C to **48000 Hz** (do not force 44100)
  3. Build: `cmake -S . -B build && cmake --build build`
  4. Run: `./build/BridgeDaemon/apm44-bridge` (document `--input-device` / `--output-device` overrides)
  5. **Logic Pro:** Output device BlackHole; project 44.1 kHz; test tone 440 Hz
  6. **Ableton Live:** Audio output BlackHole; sample rate 44100; test tone
- Troubleshooting: silence (check preflight), wrong pitch (rate mismatch), high latency (expected MVP — no presets yet).
- Buffer size note: document actual IO buffer sizes printed at bridge start.

Update `README.md` with links to both docs, quick start, and phase scope honesty (no menu bar, no drift, no custom driver yet).
  </action>
  <verify>
    <automated>test -f docs/mvp-routing.md && test -f docs/blackhole-prerequisite.md && grep -q "44100" docs/mvp-routing.md && grep -q "48000" docs/mvp-routing.md && grep -qi "logic" docs/mvp-routing.md && grep -qi "ableton" docs/mvp-routing.md</automated>
  </verify>
  <done>Routing doc covers full signal path, both DAWs, rate checklist, and GPL BlackHole policy.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize verify-devices.sh + --print-config</name>
  <files>scripts/verify-devices.sh, BridgeDaemon/src/main.cpp, BridgeDaemon/src/CliOptions.h, BridgeDaemon/src/CliOptions.cpp</files>
  <action>
Enhance `scripts/verify-devices.sh`:
- Exit code 0 only when BlackHole found at 44100 and AirPods-class output at 48000.
- Print remediation hints (open Audio MIDI Setup, download BlackHole link).
- Add `--json` optional flag for machine-readable output (uid, name, rate).

Add `apm44-bridge --print-config`: after resolution, print input UID, output UID, in/out ASBD summary, ring capacity, converter ratio — supports debugging without starting audio.

Ensure `README.md` documents verify script as gate before first run.
  </action>
  <verify>
    <automated>bash -n scripts/verify-devices.sh && cmake --build build && ./build/BridgeDaemon/apm44-bridge --help | grep -q print-config</automated>
  </verify>
  <done>Verify script syntax-valid; `--print-config` exposes resolved device/rate summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 1 success demo — 440 Hz tone</name>
  <files>docs/mvp-routing.md</files>
  <action>Pause for human end-to-end validation after automated docs and verify tooling are complete. No code changes unless user reports failure.</action>
  <verify>
    <automated>bash scripts/verify-devices.sh</automated>
  </verify>
  <done>User resume signal is "approved" after hearing 440 Hz test tone on AirPods with bridge running</done>
  <what-built>Full BlackHole → apm44-bridge → AirPods path with docs and verify tooling</what-built>
  <how-to-verify>
    1. Run `bash scripts/verify-devices.sh` — expect PASS for BlackHole @ 44100 and AirPods @ 48000
    2. Open Audio MIDI Setup — confirm BlackHole 44100 Hz and AirPods 48000 Hz
    3. Start `./build/BridgeDaemon/apm44-bridge` (leave running)
    4. In Logic or Ableton: project/sample rate **44100 Hz**, output **BlackHole 2ch**, play **440 Hz** test tone (~10 s)
    5. Confirm audible tone in AirPods (not sustained silence / not gross pitch error)
    6. Stop bridge with Ctrl+C; note xrun count printed on exit
  </how-to-verify>
  <resume-signal>Type "approved" if tone heard cleanly enough for MVP, or describe failure mode (silence, crackle, wrong pitch)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | User follows external BlackHole install URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | S | blackhole-prerequisite.md links | mitigate | Link to official Existential Audio / GitHub releases only |
</threat_model>

<verification>
- All Phase 1 ROADMAP success criteria addressable via docs + demo
- `scripts/verify-devices.sh` returns meaningful exit codes
</verification>

<success_criteria>
- MVP-03 documentation complete
- Human approves 440 Hz audible monitoring demo
- Phase 1 ready for `/gsd-verify-work` or Phase 2 planning
</success_criteria>

<output>
After completion, create `.planning/phases/01-blackhole-console-bridge/01-04-SUMMARY.md`
</output>
