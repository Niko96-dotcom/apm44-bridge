---
phase: 30-signing-and-notary-fail-closed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/notarize-hal-driver.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - NOTARY-01
  - NOTARY-02
  - NOTARY-03
must_haves:
  truths:
    - "`scripts/notarize-hal-driver.sh` sources `scripts/notary-result.sh`."
    - "HAL driver notarization calls `require_notary_accepted`."
    - "Rejected driver-only notarization does not staple."
---

<objective>
Route HAL driver notarization through the shared strict notary helper and preserve stapling after accepted notarization.
</objective>

<tasks>

<task type="auto">
  <name>Use shared strict notary helper</name>
  <read_first>scripts/notarize-hal-driver.sh, scripts/notary-result.sh</read_first>
  <action>Source `scripts/notary-result.sh` and replace direct `xcrun notarytool submit` with `require_notary_accepted "$ZIP" "$PROFILE" "HAL driver"`.</action>
  <acceptance_criteria>
    - `scripts/notarize-hal-driver.sh` contains `source "$ROOT/scripts/notary-result.sh"`.
    - `scripts/notarize-hal-driver.sh` contains `require_notary_accepted "$ZIP" "$PROFILE" "HAL driver"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Regression-test driver-only notary outcomes</name>
  <read_first>tests/test_release_scripts.sh</read_first>
  <action>Add accepted and rejected `scripts/notarize-hal-driver.sh` cases using the fake notary tool. Accepted cases must staple and validate; rejected cases must fetch notary logs and avoid stapling.</action>
  <acceptance_criteria>
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
