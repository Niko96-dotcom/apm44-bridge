---
phase: 30-signing-and-notary-fail-closed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/sign-notarize.yml
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - SIGN-01
  - SIGN-02
  - SIGN-03
  - SIGN-04
must_haves:
  truths:
    - "`sign-notarize.yml` builds both `apm44-bridge` and `APM44Bridge` before signing."
    - "Missing `APPLE_SIGN_ID` exits nonzero in the manual signing workflow."
    - "Missing `AC_NOTARY` exits nonzero when `notarize=true`."
---

<objective>
Build the HAL driver in the manual signing workflow and fail closed on missing signing/notary credentials.
</objective>

<tasks>

<task type="auto">
  <name>Update manual signing workflow</name>
  <read_first>.github/workflows/sign-notarize.yml</read_first>
  <action>Change the Release build command to `cmake --build build --target apm44-bridge APM44Bridge`. Replace successful SKIP exits for missing `APPLE_SIGN_ID` and `AC_NOTARY` with `error:` messages and `exit 1`.</action>
  <acceptance_criteria>
    - `.github/workflows/sign-notarize.yml` contains `cmake --build build --target apm44-bridge APM44Bridge`.
    - `.github/workflows/sign-notarize.yml` contains `error: APPLE_SIGN_ID secret is required`.
    - `.github/workflows/sign-notarize.yml` contains `error: AC_NOTARY keychain profile is required`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Guard signing workflow drift</name>
  <read_first>tests/test_release_scripts.sh</read_first>
  <action>Add release-script assertions for the HAL driver build target and fail-closed credential error strings. Assert the old `SKIP: APPLE_SIGN_ID` and `SKIP: AC_NOTARY` strings are absent.</action>
  <acceptance_criteria>
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
