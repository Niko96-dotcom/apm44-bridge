---
phase: 32-release-candidate-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release-validation.md
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - "Full local CI passes after all v0.8 changes."
    - "`docs/release-validation.md` records release-Mac validation commands for secrets, build, signing, notarization, stapling, Gatekeeper, installed HAL, and installed app checks."
    - "`docs/release-validation.md` records operator validation expectations for clean DMG install, HAL visibility, menu-bar app start, Cubase route, smoke/soak, and export-rate proof."
    - "v0.8 requirements traceability is complete with no code-level blockers."
---

<objective>
Run release-candidate regression and validation closure for v0.8.
</objective>

<tasks>

<task type="auto">
  <name>Record release validation command set</name>
  <read_first>docs/release-validation.md, .planning/REQUIREMENTS.md</read_first>
  <action>Add release-Mac and target-hardware operator validation sections with exact commands for secrets, CI, release build, signing, notarization, stapling, Gatekeeper assessment, installed HAL/app sync checks, clean DMG install, menu-bar app start, Cubase route, soak, and export-rate proof.</action>
  <acceptance_criteria>
    - `docs/release-validation.md` contains `## Release-Mac validation commands`.
    - `docs/release-validation.md` contains `## Target-hardware operator validation`.
    - `docs/release-validation.md` contains `bash scripts/verify-installed-sync.sh`.
    - `docs/release-validation.md` contains `bash scripts/validate-export-rate.sh --check-file`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Run final CI gate</name>
  <read_first>scripts/ci.sh</read_first>
  <action>Run `bash scripts/ci.sh` after all v0.8 edits and record the resulting evidence in SUMMARY and VERIFICATION artifacts.</action>
  <acceptance_criteria>
    - `bash scripts/ci.sh` exits 0.
    - Output contains `ci: OK`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash scripts/ci.sh
```
</verification>
