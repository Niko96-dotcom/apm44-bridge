---
phase: 31-public-truth-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - BridgeDaemon/src/engine/LibSamplerateSrc.cpp
  - BridgeDaemon/src/engine/BridgeMetrics.h
  - tests/test_bridge_metrics_json.cpp
  - docs/menu-bar-qa.md
autonomous: true
requirements:
  - CLEAN-01
  - SRC-01
must_haves:
  truths:
    - "No tracked legacy converter source file remains in source directories."
    - "Standard, High, and Best SRC labels map to distinct converter behavior or public latency estimates."
    - "Regression coverage fails if public SRC quality labels collapse to identical estimated behavior."
---

<objective>
Remove or account for legacy converter surfaces and reconcile SRC quality labels.
</objective>

<tasks>

<task type="auto">
  <name>Confirm legacy converter source state</name>
  <read_first>BridgeDaemon/src/engine, tests/test_bridge_metrics_json.cpp</read_first>
  <action>Verify no tracked `AudioConverterSrc` or legacy converter source remains outside ignored build output.</action>
  <acceptance_criteria>
    - `find BridgeDaemon Shared Driver App docs scripts tests .github -iname '*converter*' -o -iname '*AudioConverter*'` returns no tracked stale converter source paths.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Make SRC labels distinct</name>
  <read_first>BridgeDaemon/src/engine/LibSamplerateSrc.cpp, BridgeDaemon/src/engine/BridgeMetrics.h, tests/test_bridge_metrics_json.cpp</read_first>
  <action>Map Medium to `SRC_SINC_FASTEST`, High to `SRC_SINC_MEDIUM_QUALITY`, and Best to `SRC_SINC_BEST_QUALITY`. Set public group-delay estimates to 1.5, 2.5, and 3.0 ms and add a metrics regression proving distinct estimates.</action>
  <acceptance_criteria>
    - `ctest --test-dir build --output-on-failure -R 'test_bridge_metrics_json|test_lib_samplerate_src'` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_bridge_metrics_json test_lib_samplerate_src
ctest --test-dir build --output-on-failure -R 'test_bridge_metrics_json|test_lib_samplerate_src'
```
</verification>
