---
phase: 31-public-truth-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/install.md
  - docs/release.md
  - docs/release-validation.md
  - .github/workflows/release.yml
  - .github/workflows/sign-notarize.yml
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Release docs use current release-candidate language instead of stale v0.4 closeout wording."
    - "DMG validation commands use the current artifact version variable with `0.1.1` default."
    - "Install docs describe Safe as the new-install default."
---

<objective>
Align version, milestone, and latency documentation with current release truth.
</objective>

<tasks>

<task type="auto">
  <name>Replace stale release milestone language</name>
  <read_first>docs/release.md, docs/release-validation.md, .github/workflows/release.yml, .github/workflows/sign-notarize.yml</read_first>
  <action>Replace stale v0.4 release-closeout wording with current release-candidate/v0.8 wording.</action>
  <acceptance_criteria>
    - `rg "v0\\.4" docs .github` has no stale release-posture hits.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Clarify current artifact version story</name>
  <read_first>docs/install.md, docs/release-validation.md</read_first>
  <action>Keep `APM44Bridge-0.1.1.dmg` as the current artifact, and use `APM44_VERSION:-0.1.1` in release validation commands.</action>
  <acceptance_criteria>
    - `docs/release-validation.md` contains `APM44Bridge-${APM44_VERSION:-0.1.1}.dmg`.
    - `docs/install.md` calls `APM44Bridge-0.1.1.dmg` the current DMG-primary public release artifact.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
rg -n "v0\\.4" docs .github
```
</verification>
