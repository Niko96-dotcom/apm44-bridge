---
phase: 15-public-distribution-ux-and-security-posture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hal-driver.md
  - docs/install.md
  - docs/release.md
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - PKG-01
  - PKG-02
must_haves:
  truths:
    - "Public docs explain /apm44_bridge_ring is local-machine IPC, not an auth or privilege boundary"
    - "Docs explicitly describe shm mode 0666 implications without overclaiming security"
    - "Docs list future local IPC hardening options"
    - "Milestone records DMG-primary public distribution and PKG maintainer-only/future status"
    - "DMG admin install flow is explicit and professional for HAL driver installation"
  artifacts:
    - path: docs/hal-driver.md
      provides: "Public local IPC threat model"
      contains: "not an authentication or privilege boundary"
    - path: docs/install.md
      provides: "End-user DMG-primary install posture"
      contains: "DMG-primary"
    - path: docs/release.md
      provides: "Maintainer installer decision and release expectations"
      contains: "PKG"
    - path: .planning/PROJECT.md
      provides: "Milestone installer posture decision"
      contains: "DMG-primary"
  key_links:
    - from: docs/install.md
      to: scripts/release-all.sh
      via: "public DMG build command"
      pattern: "release-all.sh"
    - from: docs/hal-driver.md
      to: Shared/include/apm44/ShmRingLayout.h
      via: "shm object name and local IPC limits"
      pattern: "/apm44_bridge_ring"
---

<objective>
Make the public distribution docs honest about local IPC limits and explicit
about the v0.4 DMG-primary installer posture.

Purpose: Close DOC-01 through DOC-03 and PKG-01 through PKG-02.
Output: Public docs and planning notes that explain the local shared-memory
threat model, future hardening options, DMG-primary distribution, and
maintainer-only PKG status.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-public-distribution-ux-and-security-posture/15-CONTEXT.md
@docs/hal-driver.md
@docs/install.md
@docs/release.md
@README.md

<interfaces>
Public-facing docs should avoid claiming protection that does not exist today.
Use direct language:
- local-machine IPC,
- not an authentication or privilege boundary,
- mode `0666` permits broad local open permissions,
- no secrets in the ring,
- future hardening options are not current guarantees.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public local IPC security section</name>
  <files>docs/hal-driver.md, docs/release.md</files>
  <read_first>
    - docs/hal-driver.md
    - docs/release.md
    - Shared/include/apm44/ShmRingLayout.h
  </read_first>
  <acceptance_criteria>
    - `grep -n 'not an authentication or privilege boundary' docs/hal-driver.md docs/release.md` returns at least one match
    - `grep -n '0666' docs/hal-driver.md docs/release.md` returns at least one match
    - `grep -n 'Future hardening' docs/hal-driver.md docs/release.md` returns at least one match
  </acceptance_criteria>
  <action>
    Expand the HAL IPC docs and release docs with a public Security / Local IPC
    section that describes `/apm44_bridge_ring`, mode `0666`, local-user
    implications, no-secret constraints, and future hardening options such as
    per-user naming, tighter ownership, a privileged installer/helper, or
    XPC-mediated setup.
  </action>
  <verify>
    <automated>grep -n 'not an authentication or privilege boundary\|0666\|Future hardening' docs/hal-driver.md docs/release.md</automated>
  </verify>
  <done>Public docs are honest about current local IPC protection limits.</done>
</task>

<task type="auto">
  <name>Task 2: Record DMG-primary installer decision and professional admin flow</name>
  <files>docs/install.md, docs/release.md, README.md, .planning/PROJECT.md</files>
  <read_first>
    - docs/install.md
    - docs/release.md
    - README.md
    - .planning/PROJECT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n 'DMG-primary' docs/install.md docs/release.md .planning/PROJECT.md` returns at least one match
    - `grep -n 'maintainer-only' docs/install.md docs/release.md` returns at least one match
    - `grep -n 'admin password' docs/install.md` returns at least one match
  </acceptance_criteria>
  <action>
    Make the installer posture explicit:
    - v0.4 public distribution is DMG-primary.
    - The DMG intentionally uses an admin installer command because HAL drivers
      install under `/Library/Audio/Plug-Ins/HAL/`.
    - PKG tooling is maintainer-only/future until installer signing and
      validation are complete.
    - README should point users to the public install doc without adding
      workbench caveats.
  </action>
  <verify>
    <automated>grep -n 'DMG-primary\|maintainer-only\|admin password' docs/install.md docs/release.md README.md .planning/PROJECT.md</automated>
  </verify>
  <done>Installer posture is explicit, professional, and consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shared memory | `/apm44_bridge_ring` is opened by local system/user processes |
| End-user installer | DMG command runs admin operations for HAL driver installation |
| Public docs | Docs shape user expectations about security and trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | I | Shared memory docs | mitigate | Say mode `0666` is broad local IPC and no secrets belong in the ring |
| T-15-01-02 | E | Installer docs | mitigate | Explain admin password is required for HAL install under `/Library` |
| T-15-01-03 | R | Installer decision | mitigate | Record DMG-primary and PKG maintainer-only/future status |
</threat_model>

<verification>
```bash
grep -n 'not an authentication or privilege boundary\|0666\|Future hardening' docs/hal-driver.md docs/release.md
grep -n 'DMG-primary\|maintainer-only\|admin password' docs/install.md docs/release.md README.md .planning/PROJECT.md
```
</verification>

<success_criteria>
- DOC-01 through DOC-03 are implemented.
- PKG-01 and PKG-02 are implemented.
- Public docs do not overclaim local IPC security.
</success_criteria>

<output>
After completion, create `.planning/phases/15-public-distribution-ux-and-security-posture/15-01-SUMMARY.md`
</output>
