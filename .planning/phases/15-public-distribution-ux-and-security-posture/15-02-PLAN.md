---
phase: 15-public-distribution-ux-and-security-posture
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - scripts/build-release-dmg.sh
  - scripts/release-all.sh
  - docs/release.md
  - docs/install.md
  - .github/workflows/release.yml
  - .github/workflows/sign-notarize.yml
  - .planning/PROJECT.md
autonomous: true
requirements:
  - PKG-03
  - GHA-01
must_haves:
  truths:
    - "release-all packages the final DMG after inner app/driver stapling and validation"
    - "Release docs describe the same staple/validate order as release-all"
    - "Critical release/signing workflows either use full SHA pins or have an explicit trust decision"
    - "The explicit GitHub Actions trust decision mentions official actions, Dependabot monitoring, and future SHA pinning trigger"
  artifacts:
    - path: scripts/build-release-dmg.sh
      provides: "Package-only DMG mode or equivalent final-DMG packaging seam"
      contains: "APM44_DMG_PACKAGE_ONLY"
    - path: scripts/release-all.sh
      provides: "Final DMG packaged after inner stapling"
      contains: "APM44_DMG_PACKAGE_ONLY=1"
    - path: docs/release.md
      provides: "Release order and workflow trust decision"
      contains: "GitHub Actions trust decision"
    - path: .github/workflows/release.yml
      provides: "Workflow comment pointing to trust decision"
      contains: "trust decision"
  key_links:
    - from: scripts/release-all.sh
      to: scripts/build-release-dmg.sh
      via: "package-only final DMG after stapling"
      pattern: "APM44_DMG_PACKAGE_ONLY=1"
    - from: docs/release.md
      to: .github/workflows/sign-notarize.yml
      via: "documented workflow trust posture"
      pattern: "GitHub Actions trust decision"
---

<objective>
Align release artifact stapling order with the distributed public DMG and record
the GitHub Actions trust posture.

Purpose: Close PKG-03 and GHA-01 after docs state the installer posture.
Output: Release scripts/docs that package the final DMG from stapled inner
artifacts, plus explicit workflow trust decision coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-public-distribution-ux-and-security-posture/15-CONTEXT.md
@scripts/build-release-dmg.sh
@scripts/release-all.sh
@docs/release.md
@.github/workflows/release.yml
@.github/workflows/sign-notarize.yml
@.github/dependabot.yml

<interfaces>
Final public release order:
1. Build app/driver/daemon and embed helper.
2. Sign app, driver, daemon.
3. Submit evidence zip for app/driver acceptance.
4. Staple and validate inner app and driver.
5. Package final DMG from those stapled inner artifacts.
6. Notarize, staple, and validate the final DMG.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package final DMG after inner app/driver stapling</name>
  <files>scripts/build-release-dmg.sh, scripts/release-all.sh, tests/test_release_scripts.sh</files>
  <read_first>
    - scripts/build-release-dmg.sh
    - scripts/release-all.sh
    - scripts/notarize-release-dmg.sh
    - tests/test_release_scripts.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'APM44_DMG_PACKAGE_ONLY' scripts/build-release-dmg.sh scripts/release-all.sh` returns at least two matches
    - `grep -n 'stapler validate "build/Release/APM44 Bridge.app"' scripts/release-all.sh` returns at least one match
    - `grep -n 'stapler validate build/Driver/APM44Bridge.driver' scripts/release-all.sh` returns at least one match
    - `bash tests/test_release_scripts.sh` exits 0
  </acceptance_criteria>
  <action>
    Add a package-only mode to `build-release-dmg.sh` or an equivalent helper so
    `release-all.sh` can rebuild the final DMG from already-stapled app/driver
    artifacts without rebuilding or re-signing them. Update `release-all.sh` to
    validate inner stapled app/driver tickets before packaging the final DMG and
    then notarize/staple/validate the final DMG.

    Update mocked release-script tests if needed so release-all's local-only
    override and critical command sequence remain covered without Apple
    credentials.
  </action>
  <verify>
    <automated>bash -n scripts/build-release-dmg.sh scripts/release-all.sh tests/test_release_scripts.sh && bash tests/test_release_scripts.sh</automated>
  </verify>
  <done>The distributed DMG contains the same inner app/driver artifacts that were stapled and validated.</done>
</task>

<task type="auto">
  <name>Task 2: Document release order and GitHub Actions trust posture</name>
  <files>docs/release.md, docs/install.md, .github/workflows/release.yml, .github/workflows/sign-notarize.yml, .planning/PROJECT.md</files>
  <read_first>
    - docs/release.md
    - docs/install.md
    - .github/workflows/release.yml
    - .github/workflows/sign-notarize.yml
    - .github/dependabot.yml
    - .planning/PROJECT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n 'GitHub Actions trust decision' docs/release.md .planning/PROJECT.md` returns at least one match
    - `grep -n 'Dependabot' docs/release.md .planning/PROJECT.md` returns at least one match
    - `grep -n 'full-length SHA' docs/release.md .planning/PROJECT.md` returns at least one match
    - `grep -n 'trust decision' .github/workflows/release.yml .github/workflows/sign-notarize.yml` returns at least one match
  </acceptance_criteria>
  <action>
    Update release docs and planning notes to state the exact release order and
    the v0.4 workflow trust decision: official GitHub actions stay tag-pinned
    for now, Dependabot monitors them weekly, and full-length SHA pinning is the
    required future hardening path before moving more signing or release
    publication into GitHub-hosted automation.

    Add short workflow comments pointing maintainers to the documented trust
    decision so the action tags are not silently accepted by accident.
  </action>
  <verify>
    <automated>grep -n 'GitHub Actions trust decision\|Dependabot\|full-length SHA\|trust decision' docs/release.md .planning/PROJECT.md .github/workflows/release.yml .github/workflows/sign-notarize.yml</automated>
  </verify>
  <done>GHA-01 is satisfied by an explicit, documented trust decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Final public DMG | Users receive exactly the packaged app/driver in this container |
| GitHub Actions workflows | Release/signing workflows execute external actions near artifacts or credentials |
| Maintainer release process | Local scripts coordinate signing, notarization, stapling, and validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | T | release-all.sh | mitigate | Package final DMG after inner app/driver stapling and validation |
| T-15-02-02 | R | release docs | mitigate | Document exact release order and validation commands |
| T-15-02-03 | T | GitHub Actions | mitigate | Record explicit trust decision and future full-SHA hardening trigger |
</threat_model>

<verification>
```bash
bash -n scripts/build-release-dmg.sh scripts/release-all.sh tests/test_release_scripts.sh
bash tests/test_release_scripts.sh
grep -n 'GitHub Actions trust decision\|Dependabot\|full-length SHA\|trust decision' docs/release.md .planning/PROJECT.md .github/workflows/release.yml .github/workflows/sign-notarize.yml
```
</verification>

<success_criteria>
- PKG-03 is implemented.
- GHA-01 is implemented.
- Release scripts and public release docs agree on the artifact order.
</success_criteria>

<output>
After completion, create `.planning/phases/15-public-distribution-ux-and-security-posture/15-02-SUMMARY.md`
</output>
