---
phase: 03-menu-bar-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App/APM44Bridge.xcodeproj/project.pbxproj
  - App/APM44Bridge/APM44BridgeApp.swift
  - App/APM44Bridge/BridgeProcessManager.swift
  - App/APM44Bridge/BridgeBinaryLocator.swift
  - App/APM44Bridge/BridgeSettings.swift
  - App/APM44Bridge/MenuContentView.swift
  - App/APM44Bridge/Assets.xcassets
  - scripts/verify-app-build.sh
  - docs/menu-bar-app.md
autonomous: true
requirements:
  - APP-01
must_haves:
  truths:
    - "User sees a menu bar icon for APM44 Bridge"
    - "User can Start and Stop the bridge from the menu"
    - "Icon reflects running (active), stopped (idle), or error (launch failure)"
    - "Menu shows whether the bridge subprocess is running"
  artifacts:
    - path: App/APM44Bridge/APM44BridgeApp.swift
      provides: "MenuBarExtra entry point"
      exports: ["APM44BridgeApp"]
    - path: App/APM44Bridge/BridgeProcessManager.swift
      provides: "Subprocess spawn/stop"
    - path: scripts/verify-app-build.sh
      provides: "CI build gate for app target"
  key_links:
    - from: App/APM44Bridge/BridgeProcessManager.swift
      to: build/BridgeDaemon/apm44-bridge
      via: "Process executableURL from BridgeBinaryLocator"
      pattern: "Process\\("
---

## Phase Goal

**As a** producer using APM44 Bridge, **I want** a menu bar control to start and stop the bridge daemon, **so that** I can monitor on AirPods without running Terminal commands.

<objective>
Deliver the **vertical MVP slice**: macOS 14+ menu bar app that spawns `apm44-bridge` with Balanced defaults (15 ms, medium SRC) and shows running/stopped/error icon state.

Purpose: Satisfy APP-01 core before meters, device picker, or hotplug.
Output: `APM44Bridge.app` target under `App/`, dev launch docs, `scripts/verify-app-build.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-menu-bar-application/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@BridgeDaemon/src/CliOptions.cpp

**UI-SPEC:** `03-UI-SPEC.md` not present at plan time — follow CONTEXT shell decisions (MenuBarExtra, green/gray/red icon states). When UI-SPEC lands, align tint assets and menu copy without changing behavior contracts.

<interfaces>
From `BridgeDaemon` CLI (spawn args for MVP slice):
- `--output-device <UID>` (optional; omit until plan 03-04 wires picker — use daemon default AirPods match)
- `--target-fill-ms 15`
- `--src-quality medium`

From `BridgeEngine` / Phase 2: binary at `build/BridgeDaemon/apm44-bridge` after CMake build.

`BridgeBinaryLocator` resolution order (per CONTEXT discretion — subprocess MVP):
1. `ProcessInfo.processInfo.environment["APM44_BRIDGE_PATH"]`
2. `Bundle.main.url(forAuxiliaryExecutable: "apm44-bridge")` (placeholder for future copy phase)
3. Repo-relative `build/BridgeDaemon/apm44-bridge` when `#if DEBUG` or env `APM44_DEV_REPO_ROOT`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xcode menu bar target and build verification script</name>
  <files>App/APM44Bridge.xcodeproj/project.pbxproj, App/APM44Bridge/APM44BridgeApp.swift, App/APM44Bridge/Assets.xcassets, scripts/verify-app-build.sh, docs/menu-bar-app.md</files>
  <action>
Create `App/APM44Bridge.xcodeproj` with macOS app target **APM44Bridge** (`PRODUCT_BUNDLE_IDENTIFIER` e.g. `com.niko.apm44.menu`, `MACOSX_DEPLOYMENT_TARGET = 14.0`, Swift 5.10+).

- `APM44BridgeApp.swift`: `@main` struct with `MenuBarExtra("APM44 Bridge", systemImage: "headphones")` — use `MenuContentView` as label content.
- Asset catalog: three template images or SF Symbol mapping — **running** = green tint, **stopped** = secondary/gray, **error** = red (CONTEXT app shell).
- `scripts/verify-app-build.sh`: `set -euo pipefail`; `xcodebuild -project App/APM44Bridge.xcodeproj -scheme APM44Bridge -configuration Debug build`; exit 0 on success.
- `docs/menu-bar-app.md`: dev prerequisites (Xcode 16, CMake build of daemon), set `APM44_BRIDGE_PATH`, open app from Xcode.

No HAL, no XPC service, no Swift in audio callbacks.
  </action>
  <verify>
    <automated>bash scripts/verify-app-build.sh</automated>
  </verify>
  <done>Debug build succeeds; app appears in menu bar when run from Xcode; doc lists env var for daemon path.</done>
</task>

<task type="auto">
  <name>Task 2: BridgeProcessManager — spawn, stop, icon state machine</name>
  <files>App/APM44Bridge/BridgeProcessManager.swift, App/APM44Bridge/BridgeBinaryLocator.swift, App/APM44Bridge/BridgeSettings.swift, App/APM44Bridge/MenuContentView.swift</files>
  <action>
Implement subprocess control per CONTEXT (MVP: subprocess + pipes, not XPC):

**`BridgeProcessManager`** (`@MainActor`, `ObservableObject`):
- States: `idle`, `running`, `error(String)`
- `start()`: resolve binary via `BridgeBinaryLocator`; `Process` with `executableURL`, arguments `["--target-fill-ms","15","--src-quality","medium"]` plus optional `--output-device` from `BridgeSettings.outputDeviceUid` when set.
- Capture stdout/stderr on background queue (store last 20 lines for error tooltip); **do not parse metrics yet** (plan 03-03).
- `stop()`: `terminate()` then `waitUntilExit()` with timeout; reset to `idle`.
- On launch failure (missing binary, immediate exit != 0): `error` state with message.

**`MenuContentView`**: Start/Stop toggle; status text ("Running" / "Stopped" / error summary); placeholder line "Output: (default)" until plan 03-04.

**`BridgeSettings`**: `UserDefaults` keys `outputDeviceUid` (optional String).

Wire `MenuBarExtra` label/icon to `BridgeProcessManager` state (APP-01: running/stopped visible).

**Avoid:** claiming zero latency; opening AirPods from Swift (daemon only).
  </action>
  <verify>
    <automated>bash scripts/verify-app-build.sh && test -f App/APM44Bridge/BridgeProcessManager.swift</automated>
  </verify>
  <done>With daemon built and `APM44_BRIDGE_PATH` set, Start launches subprocess and icon shows running; Stop returns to stopped; missing binary shows error state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Menu app → `apm44-bridge` subprocess | Untrusted child stdout/stderr; no shell interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | BridgeBinaryLocator | mitigate | Resolve only explicit paths/env; never `shell` with user-provided strings |
| T-03-02 | Denial of Service | BridgeProcessManager | accept | Local UX only; user can Force Quit |
| T-03-03 | Information Disclosure | stderr capture | accept | Local troubleshooting buffer only |
</threat_model>

<verification>
- `scripts/verify-app-build.sh` passes in CI/local
- Manual: Start/Stop with BlackHole + AirPods connected (document in SUMMARY)
</verification>

<success_criteria>
- APP-01 partially met: running/stopped + menu bar icon states
- No regression to BridgeDaemon RT path
</success_criteria>

<output>
After completion, create `.planning/phases/03-menu-bar-application/03-01-SUMMARY.md`
</output>
