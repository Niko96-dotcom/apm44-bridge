---
phase: 03-menu-bar-application
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - App/APM44Bridge/BridgeProcessManager.swift
  - App/APM44Bridge/BridgeMetricsSnapshot.swift
  - App/APM44Bridge/MetricsParser.swift
  - App/APM44Bridge/MenuContentView.swift
  - tests/test_metrics_parser.swift
  - App/APM44Bridge.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - APP-01
  - APP-05
  - QA-03
must_haves:
  truths:
    - "Menu shows selected output device name when bridge is running"
    - "Menu shows current buffer fill and glitch indicator when metrics update"
    - "Menu shows honest monitoring latency string (never zero)"
  artifacts:
    - path: App/APM44Bridge/MetricsParser.swift
      provides: "JSON line → BridgeMetricsSnapshot"
    - path: App/APM44Bridge/BridgeMetricsSnapshot.swift
      provides: "Typed metrics model"
  key_links:
    - from: BridgeProcessManager
      to: apm44-bridge stdout
      via: "append --metrics-json; readLines on pipe"
      pattern: "metrics-json"
---

## Phase Goal

**As a** producer using APM44 Bridge, **I want** live buffer and latency readouts in the menu bar, **so that** I can troubleshoot glitches and understand monitoring delay.

<objective>
Wire **stdout JSON metrics** (plan 03-02) into the menu app: parse lines on a background queue, publish to UI at 500 ms, show fill bar, xrun glitch badge, and `~X ms monitoring latency` (APP-05, QA-03). Complete APP-01 device name line when UID known.

Purpose: Close the control-loop between daemon and SwiftUI without XPC.
Output: `MetricsParser`, live menu section, spawn includes `--metrics-json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-menu-bar-application/03-CONTEXT.md
@.planning/phases/03-menu-bar-application/03-02-PLAN.md

<interfaces>
Daemon JSON fields from `BridgeMetrics` (plan 03-02): `fill_ms`, `xruns`, `ratio`, `estimated_rt_ms`, `target_fill_ms`, `src_quality`, `underruns`, `overruns`, `ppm`.

`BridgeProcessManager` from plan 03-01: `start()` / `stop()`, `ObservableObject` state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MetricsParser unit tests (Swift)</name>
  <files>App/APM44Bridge/MetricsParser.swift, App/APM44Bridge/BridgeMetricsSnapshot.swift, tests/test_metrics_parser.swift, App/APM44Bridge.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: Valid JSON line decodes to snapshot with `fillMs == 15.2`, `estimatedRtMs > 0`.
    - Test 2: Malformed line returns nil without crashing.
    - Test 3: `latencyLabel` formats as "~12 ms monitoring latency" using `estimatedRtMs` rounded (never "0 ms" unless engine truly reports 0 fill + delay).
  </behavior>
  <action>
Add macOS unit test target **APM44BridgeTests** (or swift test package if simpler — prefer Xcode test bundle).

`BridgeMetricsSnapshot`: Codable fields matching daemon JSON.

`MetricsParser.parse(line:) -> BridgeMetricsSnapshot?`

`var latencyLabel: String` — prefix `~`, suffix ` monitoring latency` per CONTEXT honesty rule (QA-03).

Add static fixture line copied from C++ test output for regression.
  </action>
  <verify>
    <automated>xcodebuild -project App/APM44Bridge.xcodeproj -scheme APM44Bridge -destination 'platform=macOS' test -only-testing:APM44BridgeTests 2>&1 | tail -5</automated>
  </verify>
  <done>Parser tests pass; `estimatedRtMs` never forced to 0 in label helper.</done>
</task>

<task type="auto">
  <name>Task 2: Pipe reader, UI meters, spawn --metrics-json</name>
  <files>App/APM44Bridge/BridgeProcessManager.swift, App/APM44Bridge/MenuContentView.swift</files>
  <action>
Extend `BridgeProcessManager`:

- On `start()`, append `--metrics-json` to daemon args (per CONTEXT).
- `FileHandle` readability handler on stdout: accumulate lines, dispatch `MetricsParser` on background, `@MainActor` publish `latestMetrics`.
- Track `lastXrunCount`; if `xruns` increases between snapshots, set `glitchFlash = true` for 2s (APP-05 glitch indicator).

`MenuContentView` meters section (simple SwiftUI — exact layout discretion):
- `ProgressView(value:)` normalized by `target_fill_ms` from snapshot (cap at 1.0).
- Text: `latencyLabel` from snapshot.
- Glitch: SF Symbol `exclamationmark.triangle.fill` when `glitchFlash`.
- Status line: `Output: {deviceDisplayName}` — resolve name from `BridgeSettings` + cached name from last `--print-config` or placeholder UID suffix until plan 03-04.

**Prohibited:** "Zero latency", "no latency", or hiding `estimated_rt_ms`.

Poll UI refresh at metrics arrival (~500 ms), not faster busy-loop.
  </action>
  <verify>
    <automated>bash scripts/verify-app-build.sh</automated>
  </verify>
  <done>While bridge runs, menu updates fill/latency; increasing xruns shows glitch indicator; latency string uses `~N ms` form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Child stdout → MetricsParser | Untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | Line buffer | mitigate | Cap retained stdout buffer (e.g. 64 KB); drop oldest lines |
| T-03-07 | Spoofing | JSON fields | accept | Local child only; no security decision on metrics |
</threat_model>

<verification>
- Swift parser tests green
- Manual smoke with running bridge (SUMMARY)
</verification>

<success_criteria>
- APP-05 and QA-03 partially met via live meters + honest latency copy
- APP-01 shows output device label when settings populated
</success_criteria>

<output>
After completion, create `.planning/phases/03-menu-bar-application/03-03-SUMMARY.md`
</output>
