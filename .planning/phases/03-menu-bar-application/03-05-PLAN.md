---
phase: 03-menu-bar-application
plan: 05
type: execute
wave: 3
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - App/APM44Bridge/LatencyPreset.swift
  - App/APM44Bridge/SrcQuality.swift
  - App/APM44Bridge/BridgeSettings.swift
  - App/APM44Bridge/MenuContentView.swift
  - App/APM44Bridge/BridgeProcessManager.swift
  - tests/test_latency_preset.swift
autonomous: true
requirements:
  - APP-02
  - APP-03
must_haves:
  truths:
    - "User can select Low, Balanced (default), or Safe latency presets"
    - "Each preset maps to the correct --target-fill-ms and default --src-quality"
    - "User can override SRC quality tier (medium/high/best) independent of preset default"
  artifacts:
    - path: App/APM44Bridge/LatencyPreset.swift
      provides: "Preset → CLI args"
  key_links:
    - from: LatencyPreset
      to: apm44-bridge CLI
      via: "target-fill-ms and src-quality args"
      pattern: "target-fill-ms"
---

## Phase Goal

**As a** producer using APM44 Bridge, **I want** latency and SRC quality presets in the menu bar, **so that** I can trade monitoring delay against stability without memorizing CLI flags.

<objective>
Implement **latency modes** and **SRC quality picker** exactly per CONTEXT locked mapping, with APP-03 override.

Purpose: APP-02 + APP-03.
Output: `LatencyPreset`, `SrcQuality`, wired spawn args, UserDefaults persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-menu-bar-application/03-CONTEXT.md

**Locked preset mapping (CONTEXT — implement exactly):**
| Mode | `--target-fill-ms` | default `--src-quality` |
|------|-------------------|-------------------------|
| Low | **8** | medium |
| Balanced (default) | **15** | medium |
| Safe | **30** | best |

ROADMAP ranges (~6–8 / ~12–18 / ~25–40) satisfied by these targets + plan 03-02 CLI clamp 6–40.

APP-03: separate Picker for `medium` | `high` | `best`; changing preset updates **suggested** quality but user override persists until they change quality picker.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LatencyPreset and SrcQuality CLI mapping tests</name>
  <files>App/APM44Bridge/LatencyPreset.swift, App/APM44Bridge/SrcQuality.swift, tests/test_latency_preset.swift</files>
  <behavior>
    - Test 1: `.balanced.targetFillMs == 15`, `.low == 8`, `.safe == 30`.
    - Test 2: `.balanced.defaultSrcQuality == .medium`, `.safe == .best`.
    - Test 3: `SrcQuality.cliFlag` yields `medium|high|best` strings matching daemon parser.
  </behavior>
  <action>
`enum LatencyPreset: String, CaseIterable` with `targetFillMs: Double` and `defaultSrcQuality: SrcQuality`.

`enum SrcQuality: String` → `cliArgument` for `--src-quality`.

`BridgeSettings`: `latencyPreset` (default `.balanced`), `srcQualityOverride: SrcQuality?` — effective quality = override ?? preset.defaultSrcQuality.

Unit tests only; no UI yet.
  </action>
  <verify>
    <automated>xcodebuild -project App/APM44Bridge.xcodeproj -scheme APM44Bridge -destination 'platform=macOS' test -only-testing:APM44BridgeTests/LatencyPreset 2>&1 | tail -5</automated>
  </verify>
  <done>All three preset ms values and quality defaults match CONTEXT table.</done>
</task>

<task type="auto">
  <name>Task 2: Menu pickers and BridgeProcessManager args</name>
  <files>App/APM44Bridge/MenuContentView.swift, App/APM44Bridge/BridgeSettings.swift, App/APM44Bridge/BridgeProcessManager.swift</files>
  <action>
UI section **Latency** (Picker segmented or menu style per discretion / future UI-SPEC):
- Low / Balanced / Safe — labels include approximate ms hint: "Low (~8 ms)", "Balanced (~15 ms)", "Safe (~30 ms)" (honest, not zero).

UI section **SRC quality**: Medium / High / Best — maps to CLI.

On preset change: if no user override active, set quality to preset default; if override active, keep override (APP-03).

`BridgeProcessManager.start()` builds args:
```
--target-fill-ms {effectiveMs}
--src-quality {effectiveQuality.cliArgument}
--output-device {uid}
--metrics-json
```

Changing preset or quality while running: stop/start cycle.

Persist `latencyPreset` and `srcQualityOverride` in UserDefaults.

**Do not** simplify to static-only presets without quality override (scope reduction prohibited).
  </action>
  <verify>
    <automated>bash scripts/verify-app-build.sh && rg -n "target-fill-ms" App/APM44Bridge/BridgeProcessManager.swift | head -3</automated>
  </verify>
  <done>Spawned process receives correct ms/quality for each preset; Balanced is default on fresh install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserDefaults | Local preferences only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Elevation | CLI args | accept | User already controls local audio daemon |
</threat_model>

<verification>
- Unit tests for preset table
- Manual: `ps` or log args on start showing 8/15/30 ms (SUMMARY)
</verification>

<success_criteria>
- APP-02 and APP-03 fully implemented per CONTEXT
</success_criteria>

<output>
After completion, create `.planning/phases/03-menu-bar-application/03-05-SUMMARY.md`
</output>
