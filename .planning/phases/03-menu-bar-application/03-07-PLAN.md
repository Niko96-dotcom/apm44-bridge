---
phase: 03-menu-bar-application
plan: 07
type: execute
wave: 5
depends_on:
  - "03-03"
  - "03-05"
  - "03-06"
files_modified:
  - scripts/verify-menu-bar.sh
  - docs/menu-bar-qa.md
  - App/APM44Bridge.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - APP-05
  - QA-03
must_haves:
  truths:
    - "Automated script verifies app build, parser tests, and daemon metrics flag"
    - "Human checklist confirms full phase success criteria on real hardware"
  artifacts:
    - path: scripts/verify-menu-bar.sh
      provides: "Phase 3 CI gate"
    - path: docs/menu-bar-qa.md
      provides: "UAT steps for APP/QA requirements"
  key_links:
    - from: scripts/verify-menu-bar.sh
      to: App + BridgeDaemon
      via: "xcodebuild + ctest + grep invariants"
      pattern: "verify-menu-bar"
---

## Phase Goal

**As a** producer using APM44 Bridge, **I want** confidence the menu bar app meets all control and monitoring requirements, **so that** I can rely on it for daily DAW sessions.

<objective>
Ship **phase verification**: `scripts/verify-menu-bar.sh` aggregates build/tests/static checks; `docs/menu-bar-qa.md` human UAT; blocking checkpoint for hardware validation.

Purpose: Close APP-01–05 and QA-03 with evidence.
Output: CI-friendly script + QA doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-menu-bar-application/03-CONTEXT.md
@docs/menu-bar-app.md

Phase success criteria (ROADMAP):
1. Icon + output device name
2. Latency modes Low/Balanced/Safe
3. SRC quality tiers
4. Hotplug without DAW restart when feasible
5. Meters + honest latency
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-menu-bar.sh and static invariants</name>
  <files>scripts/verify-menu-bar.sh, docs/menu-bar-qa.md</files>
  <action>
`scripts/verify-menu-bar.sh`:
1. `bash scripts/verify-app-build.sh`
2. `cmake --build build` (if build dir exists) + `ctest --test-dir build -R 'bridge_metrics|LatencyPreset|DeviceCatalog|MetricsParser' --output-on-failure` (skip missing tests gracefully with message)
3. Grep invariants (comment-safe):
   - `rg -n "zero latency|0 ms monitoring" App/` must be empty (QA-03)
   - `rg -n "metrics-json" App/APM44Bridge/BridgeProcessManager.swift` must match
   - `rg -n "target-fill-ms" App/APM44Bridge/LatencyPreset.swift` must match Low 8 / Balanced 15 / Safe 30

`docs/menu-bar-qa.md` checklist mapping each requirement:
- APP-01 … APP-05, QA-03 with step-by-step (BlackHole 44.1, DAW playing, AirPods output, toggle presets, disconnect test, read latency string).

Exit 0 only if automated sections pass.
  </action>
  <verify>
    <automated>bash scripts/verify-menu-bar.sh</automated>
  </verify>
  <done>Script runs locally; QA doc lists all five ROADMAP success criteria.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hardware UAT — menu bar phase sign-off</name>
  <files>docs/menu-bar-qa.md</files>
  <action>
Execute the human checklist in `docs/menu-bar-qa.md` on hardware with BlackHole @ 44.1 kHz and AirPods Max USB-C @ 48 kHz. Record pass/fail per APP-01–05 and QA-03 in the plan SUMMARY. Do not mark phase complete until all steps pass or blockers are filed.
  </action>
  <verify>
    <automated>test -f docs/menu-bar-qa.md</automated>
  </verify>
  <done>User signals "approved" after successful hardware UAT, or lists failing steps for follow-up.</done>
  <what-built>APM44Bridge menu app with start/stop, device picker, latency/SRC presets, live meters, hotplug restart, honest latency label.</what-built>
  <how-to-verify>
1. Build daemon: `cmake -B build && cmake --build build`
2. Set `export APM44_BRIDGE_PATH="$PWD/build/BridgeDaemon/apm44-bridge"`
3. Run app from Xcode; confirm menu bar icon appears
4. Select AirPods Max USB-C output; Start bridge — icon green/running, audio audible from DAW → BlackHole @ 44.1
5. Switch latency Low → Balanced → Safe; confirm no sustained crackle in each mode (~30 s each)
6. Change SRC quality to High; confirm bridge restarts and still plays
7. Confirm meter shows fill bar movement and `~N ms monitoring latency` (N > 0, not "zero")
8. Disconnect AirPods ≥2 s, reconnect; within ~2 s after debounce bridge auto-restarts if it was running; DAW still playing without restart
9. Run `bash scripts/verify-menu-bar.sh` — all automated checks pass
  </how-to-verify>
  <resume-signal>Type "approved" or list failing step numbers</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA script | Local dev machine only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Repudiation | UAT | accept | Human checkpoint records approval in SUMMARY |
</threat_model>

<verification>
- `bash scripts/verify-menu-bar.sh` exit 0
- Human checkpoint approved
</verification>

<success_criteria>
- All APP-01–05 and QA-03 traced in menu-bar-qa.md with pass/fail
- Phase 3 ready for Phase 4 HAL work
</success_criteria>

<output>
After completion, create `.planning/phases/03-menu-bar-application/03-07-SUMMARY.md`
</output>
