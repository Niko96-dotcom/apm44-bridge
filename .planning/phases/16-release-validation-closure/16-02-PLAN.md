---
phase: 16-release-validation-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - docs/release-validation.md
  - .planning/PROJECT.md
  - .planning/phases/16-release-validation-closure/16-02-SUMMARY.md
  - .planning/phases/16-release-validation-closure/16-VERIFICATION.md
autonomous: true
requirements:
  - QA-03
  - QA-04
  - QA-05
must_haves:
  truths:
    - "Final DMG artifact path is assessed with codesign, stapler validate, and spctl commands when the artifact exists"
    - "Apple credential, certificate, hardware, or operator blockers are recorded with exact unblock commands"
    - "Planning state records satisfied requirements, accepted gaps, and public-release caveats"
    - "Optional PKG path remains maintainer-only and uses pkgutil/productsign validation commands only when intentionally enabled"
  artifacts:
    - path: docs/release-validation.md
      provides: "Artifact assessment commands and unblock commands"
      contains: "Unblock commands"
    - path: .planning/phases/16-release-validation-closure/16-02-SUMMARY.md
      provides: "Live artifact assessment evidence"
      contains: "Gatekeeper"
    - path: .planning/phases/16-release-validation-closure/16-VERIFICATION.md
      provides: "Phase-level verification closure"
      contains: "status: passed"
  key_links:
    - from: docs/release-validation.md
      to: scripts/release-all.sh
      via: "public DMG validation command"
      pattern: "bash scripts/release-all.sh"
    - from: docs/release-validation.md
      to: scripts/codesign-verify-release.sh
      via: "codesign validation command"
      pattern: "bash scripts/codesign-verify-release.sh"
---

<objective>
Assess the selected DMG-primary artifact path, record exact blockers/unblock
commands, and close Phase 16 verification.

Purpose: satisfy QA-03, QA-04, and QA-05 with honest evidence from this machine.
Output: updated validation checklist, Phase 16 artifact summary, and phase-level
verification ready for `gsd-sdk query phase.complete 16`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/16-release-validation-closure/16-CONTEXT.md
@.planning/phases/16-release-validation-closure/16-01-SUMMARY.md
@docs/release-validation.md
@docs/release.md
@scripts/release-all.sh
@scripts/build-release-dmg.sh
@scripts/codesign-verify-release.sh
@scripts/notarize-release-dmg.sh
@scripts/verify-installed-sync.sh

<interfaces>
DMG-primary artifact validation commands:
- `bash scripts/codesign-verify-release.sh`
- `codesign --verify --verbose build/signing/APM44Bridge-0.1.1.dmg`
- `xcrun stapler validate "build/Release/APM44 Bridge.app"`
- `xcrun stapler validate build/Driver/APM44Bridge.driver`
- `xcrun stapler validate build/signing/APM44Bridge-0.1.1.dmg`
- `spctl --assess --type open --verbose=4 build/signing/APM44Bridge-0.1.1.dmg`

Blocker detection commands:
- `security find-identity -v -p codesigning`
- `xcrun notarytool history --keychain-profile "${NOTARY_PROFILE:-AC_NOTARY}"`
- `ls -la build/signing/*.dmg build/signing/*.pkg`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run artifact and credential assessment</name>
  <files>docs/release-validation.md, .planning/phases/16-release-validation-closure/16-02-SUMMARY.md</files>
  <read_first>
    - docs/release-validation.md
    - scripts/release-all.sh
    - scripts/build-release-dmg.sh
    - scripts/codesign-verify-release.sh
    - scripts/notarize-release-dmg.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'Unblock commands' docs/release-validation.md` returns at least one match
    - `grep -n 'security find-identity -v -p codesigning' docs/release-validation.md` returns at least one match
    - `grep -n 'xcrun notarytool store-credentials' docs/release-validation.md` returns at least one match
    - `grep -n 'spctl --assess --type open' docs/release-validation.md` returns at least one match
    - `grep -n 'Gatekeeper' .planning/phases/16-release-validation-closure/16-02-SUMMARY.md` returns at least one match
  </acceptance_criteria>
  <action>
    Run local preflight commands for signing identity, notary profile, existing
    release artifacts, and available artifact validation commands. If a public
    notarized artifact cannot be produced because credentials or certificates
    are missing, record that as blocked with exact unblock commands rather than
    treating it as a failure of the local Phase 16 work.

    Update `docs/release-validation.md` with exact unblock commands for:
    Developer ID Application identity, notary profile setup, final DMG
    stapler/Gatekeeper assessment, optional PKG validation, and hardware/operator
    evidence if a maintainer chooses to re-run live install checks.
  </action>
  <verify>
    <automated>grep -n 'Unblock commands\|security find-identity -v -p codesigning\|xcrun notarytool store-credentials\|spctl --assess --type open' docs/release-validation.md</automated>
  </verify>
  <done>Every unavailable public-release validation step has an exact unblock command.</done>
</task>

<task type="auto">
  <name>Task 2: Close validation state</name>
  <files>.planning/PROJECT.md, .planning/phases/16-release-validation-closure/16-VERIFICATION.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/16-release-validation-closure/16-01-SUMMARY.md
    - .planning/phases/16-release-validation-closure/16-02-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - `grep -n '^status: passed' .planning/phases/16-release-validation-closure/16-VERIFICATION.md` returns at least one match
    - `grep -n 'QA-01\|QA-02\|QA-03\|QA-04\|QA-05' .planning/phases/16-release-validation-closure/16-VERIFICATION.md` returns at least five matches
    - `gsd-sdk query verify.schema-drift 16` reports no blocking drift
    - `gsd-sdk query verify.codebase-drift` reports no action-required structural drift
    - `gsd-sdk query state.validate` reports valid state after phase completion
  </acceptance_criteria>
  <action>
    Create `16-VERIFICATION.md` with `status: passed`, must-have evidence for
    QA-01 through QA-05, residual caveats, and exact unblock commands. Run
    schema/codebase drift gates, complete the phase with
    `gsd-sdk query phase.complete 16`, and validate final GSD state.
  </action>
  <verify>
    <automated>gsd-sdk query verify.schema-drift 16 && gsd-sdk query verify.codebase-drift && gsd-sdk query state.validate</automated>
  </verify>
  <done>Phase 16 is complete and the milestone is ready for lifecycle audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public DMG | Primary release artifact users download |
| Apple notary service | External acceptance and ticket stapling source |
| Maintainer machine | Holds certificates, notary profile, and local build artifacts |
| Optional hardware validation | Requires real USB-C AirPods/Cubase/operator access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | R | release evidence | mitigate | Record exact local command output and blockers in summaries/verification |
| T-16-02-02 | T | public artifact | mitigate | Validate signatures, stapler tickets, and Gatekeeper result when credentials allow artifact creation |
| T-16-02-03 | R | blocked external checks | mitigate | Record exact unblock commands instead of marking blocked checks complete |
</threat_model>

<verification>
```bash
grep -n 'Unblock commands\|security find-identity -v -p codesigning\|xcrun notarytool store-credentials\|spctl --assess --type open' docs/release-validation.md
gsd-sdk query verify.schema-drift 16
gsd-sdk query verify.codebase-drift
gsd-sdk query state.validate
```
</verification>

<success_criteria>
- QA-03 is satisfied by DMG-primary artifact assessment commands and live evidence or an explicit credential blocker.
- QA-04 is satisfied by exact unblock commands for every external blocker.
- QA-05 is satisfied by GSD completion state and recorded caveats.
</success_criteria>

<output>
After completion, create `.planning/phases/16-release-validation-closure/16-02-SUMMARY.md`
and `.planning/phases/16-release-validation-closure/16-VERIFICATION.md`.
</output>
