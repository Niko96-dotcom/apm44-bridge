---
phase: 16-release-validation-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release-validation.md
  - docs/release.md
  - .planning/phases/16-release-validation-closure/16-01-SUMMARY.md
autonomous: true
requirements:
  - QA-01
  - QA-02
must_haves:
  truths:
    - "Final automated verification includes secret scan, CMake/Catch2 tests, Swift app tests, app build verification, and release-script regression tests"
    - "Release validation sequence is documented from clean build through signing, notarization, stapling, final DMG packaging, and Gatekeeper assessment"
    - "Local-only unnotarized validation is labeled as not public-release-ready"
  artifacts:
    - path: docs/release-validation.md
      provides: "Repeatable release validation checklist"
      contains: "Final automated verification"
    - path: docs/release-validation.md
      provides: "Clean build through Gatekeeper command sequence"
      contains: "Gatekeeper assessment"
    - path: docs/release.md
      provides: "Pointer to release validation record"
      contains: "release-validation.md"
  key_links:
    - from: docs/release.md
      to: docs/release-validation.md
      via: "maintainer validation checklist"
      pattern: "release-validation.md"
---

<objective>
Create the repeatable release validation checklist and run the final
credential-free automated gate.

Purpose: satisfy QA-01 and the command-sequence portion of QA-02 without
pretending Apple credential-dependent steps have run.
Output: `docs/release-validation.md`, a `docs/release.md` pointer, and a
Phase 16 summary containing the actual automated gate evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/16-release-validation-closure/16-CONTEXT.md
@scripts/ci.sh
@scripts/release-all.sh
@scripts/codesign-verify-release.sh
@scripts/notarize-release-dmg.sh
@docs/release.md

<interfaces>
Final public release sequence:
1. Remove stale release/build artifacts or start from a clean checkout.
2. Run `bash scripts/ci.sh`.
3. Verify Developer ID Application identity availability.
4. Verify notary profile availability.
5. Run `bash scripts/release-all.sh` for the notarized public DMG.
6. Verify app, driver, and DMG signatures.
7. Validate stapled tickets on app, driver, and final DMG.
8. Assess final DMG with Gatekeeper using `spctl --assess --type open`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release validation checklist</name>
  <files>docs/release-validation.md, docs/release.md</files>
  <read_first>
    - docs/release.md
    - scripts/ci.sh
    - scripts/release-all.sh
    - scripts/codesign-verify-release.sh
    - scripts/notarize-release-dmg.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n 'Final automated verification' docs/release-validation.md` returns at least one match
    - `grep -n 'bash scripts/ci.sh' docs/release-validation.md` returns at least one match
    - `grep -n 'bash scripts/release-all.sh' docs/release-validation.md` returns at least one match
    - `grep -n 'Gatekeeper assessment' docs/release-validation.md` returns at least one match
    - `grep -n 'release-validation.md' docs/release.md` returns at least one match
  </acceptance_criteria>
  <action>
    Create `docs/release-validation.md` with a maintainer-facing checklist that
    explicitly covers final automated verification, clean release build,
    Developer ID signing, notary submission, app/driver stapling and validation,
    final DMG packaging, DMG notarization/stapling/validation, and Gatekeeper
    assessment.

    Add a short pointer in `docs/release.md` to the release validation checklist.
  </action>
  <verify>
    <automated>grep -n 'Final automated verification\|bash scripts/ci.sh\|bash scripts/release-all.sh\|Gatekeeper assessment' docs/release-validation.md && grep -n 'release-validation.md' docs/release.md</automated>
  </verify>
  <done>The release validation sequence is documented in one repeatable place.</done>
</task>

<task type="auto">
  <name>Task 2: Run final automated gate</name>
  <files>.planning/phases/16-release-validation-closure/16-01-SUMMARY.md</files>
  <read_first>
    - scripts/ci.sh
    - tests/test_release_scripts.sh
    - .planning/phases/16-release-validation-closure/16-01-PLAN.md
  </read_first>
  <acceptance_criteria>
    - `bash scripts/ci.sh` exits 0
    - `grep -n 'ci: OK' .planning/phases/16-release-validation-closure/16-01-SUMMARY.md` returns at least one match
    - `grep -n 'Swift unit tests' .planning/phases/16-release-validation-closure/16-01-SUMMARY.md` returns at least one match
    - `grep -n 'release-script regression tests' .planning/phases/16-release-validation-closure/16-01-SUMMARY.md` returns at least one match
  </acceptance_criteria>
  <action>
    Run `bash scripts/ci.sh` from a clean repo state except for the Phase 16
    documentation work. Capture the high-signal result in the plan summary:
    secret scan, native build/tests, release-script tests, Swift app build,
    Swift unit tests, helper embedding, installed-sync dry-run, and final
    `ci: OK`.
  </action>
  <verify>
    <automated>bash scripts/ci.sh</automated>
  </verify>
  <done>QA-01 has fresh full-CI evidence from this Phase 16 run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CI gate | Credential-free proof before release credentials are trusted |
| Maintainer docs | Humans follow the documented release command sequence |
| Apple notarization | External service determines public artifact acceptance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | R | release validation docs | mitigate | Record exact commands and generated evidence rather than informal release notes |
| T-16-01-02 | T | release artifact path | mitigate | Require final automated gate before public release command sequence |
| T-16-01-03 | R | local-only artifacts | mitigate | Label unnotarized override output as not public-release-ready |
</threat_model>

<verification>
```bash
grep -n 'Final automated verification\|bash scripts/ci.sh\|bash scripts/release-all.sh\|Gatekeeper assessment' docs/release-validation.md
grep -n 'release-validation.md' docs/release.md
bash scripts/ci.sh
```
</verification>

<success_criteria>
- QA-01 is verified by a fresh `scripts/ci.sh` run.
- QA-02 has a documented release validation command sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/16-release-validation-closure/16-01-SUMMARY.md`.
</output>
