---
phase: 04-hal-virtual-device
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - Driver/CMakeLists.txt
  - Driver/src/APM44Plugin.cpp
  - Driver/src/APM44Device.cpp
  - Driver/src/APM44Device.h
  - Driver/src/ShmIoHandler.h
  - Driver/src/ShmIoHandler.cpp
  - Driver/Info.plist.in
  - CMakeLists.txt
autonomous: true
requirements:
  - DRV-01
  - DRV-02
  - DEV-01
  - DEV-02
must_haves:
  truths:
    - "APM44Bridge.driver bundle builds and contains CFPlugIn factory entry"
    - "Device advertises 44100 Hz, 2ch float output named APM44 Bridge"
    - "IO path writes DAW audio into mmap ring (no AirPods, no SRC)"
  artifacts:
    - path: Driver/APM44Bridge.driver
      provides: "HAL plug-in bundle (build output)"
    - path: Driver/src/APM44Device.cpp
      provides: "libASPL device + shm producer"
  key_links:
    - from: ShmIoHandler.cpp
      to: Shared/include/apm44/MmapShmRing.h
      pattern: "MmapShmRing"
---

<objective>
Deliver **minimal HAL driver skeleton** using libASPL: virtual output @ 44100 that copies render buffers into the shm ring (DRV-01/02, DEV-01/02). Full coreaudiod QA may remain manual.
</objective>

<context>
@.planning/phases/04-hal-virtual-device/04-CONTEXT.md
@.planning/phases/04-hal-virtual-device/04-RESEARCH.md
@cmake/LibASPL.cmake
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver CMake bundle target</name>
  <files>Driver/CMakeLists.txt, Driver/Info.plist.in, CMakeLists.txt</files>
  <action>
1. `add_subdirectory(Driver)` from root CMakeLists after LibASPL.

2. Build `APM44Bridge` as `MODULE` or `BUNDLE` with `.driver` extension, `Info.plist` with:
   - `CFBundleIdentifier` com.niko.apm44.bridge
   - `CFPlugInFactories` / `CFPlugInTypes` for AudioServerPlugIn

3. Link `libASPL`, `apm44_shared` (shm only — no CoreAudio in hot path beyond libASPL).

4. Output: `${CMAKE_BINARY_DIR}/Driver/APM44Bridge.driver`
  </action>
  <verify>
    <automated>cmake --build build --target APM44Bridge 2>&1 | tail -5</automated>
  </verify>
  <done>Driver target compiles on macOS.</done>
</task>

<task type="auto">
  <name>Task 2: libASPL device + shm producer IO</name>
  <files>Driver/src/APM44Plugin.cpp, Driver/src/APM44Device.cpp, Driver/src/APM44Device.h, Driver/src/ShmIoHandler.cpp, Driver/src/ShmIoHandler.h</files>
  <action>
Per CONTEXT:
- UID `com.niko.apm44.bridge.device`, name `APM44 Bridge`
- Nominal rates: 44100 only
- Format: 2ch float non-interleaved (match `AudioFormats` helpers if useful)

`ShmIoHandler`: owns `MmapShmRing` as Producer; on render callback copy input buffers to shm push (RT-safe: no alloc/log).

Register stream IO handler with libASPL — follow `examples/` pattern from libASPL for output stream.

**Skeleton allowance:** If full stream registration is blocked by API discovery time, implement `ProcessOutput` stub that still pushes silence/sine for bring-up and document gap in driver README comment.

Do NOT open AirPods or call SRC.
  </action>
  <verify>
    <automated>cmake --build build --target APM44Bridge && test -f build/Driver/APM44Bridge.driver/Contents/MacOS/*</automated>
  </verify>
  <done>Binary exists; device metadata constants match CONTEXT.</done>
</task>

</tasks>

<verification>
- `file build/Driver/APM44Bridge.driver/Contents/MacOS/*` shows Mach-O bundle binary
- `rg com.niko.apm44.bridge Driver/` has UID string
</verification>

<success_criteria>
- Buildable driver skeleton ready for `install-driver.sh`
</success_criteria>
