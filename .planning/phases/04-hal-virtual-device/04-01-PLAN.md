---
phase: 04-hal-virtual-device
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - third_party/libASPL
  - third_party/README.md
  - cmake/LibASPL.cmake
  - CMakeLists.txt
autonomous: true
requirements:
  - DRV-01
must_haves:
  truths:
    - "libASPL is vendored at a pinned git tag (v3.1.2)"
    - "CMake exposes libASPL static target for Driver/"
  artifacts:
    - path: cmake/LibASPL.cmake
      provides: "Pinned libASPL subdirectory"
    - path: third_party/README.md
      provides: "libASPL version + MIT license row"
  key_links:
    - from: CMakeLists.txt
      to: cmake/LibASPL.cmake
      pattern: "LibASPL"
---

<objective>
Vendor **libASPL v3.1.2** as a git submodule and wire CMake so `Driver/` can link the static library (DRV-01 foundation).
</objective>

<context>
@.planning/phases/04-hal-virtual-device/04-CONTEXT.md
@.planning/phases/04-hal-virtual-device/04-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add libASPL submodule at v3.1.2</name>
  <files>.gitmodules, third_party/libASPL, third_party/README.md</files>
  <action>
1. `git submodule add https://github.com/gavv/libASPL.git third_party/libASPL` (or init if present) and checkout tag **v3.1.2**.

2. Update `third_party/README.md` with libASPL row (MIT, pin command).

3. Create `cmake/LibASPL.cmake`:
   - `add_subdirectory(third_party/libASPL EXCLUDE_FROM_ALL)` 
   - Disable libASPL tests if CMake options exist
   - Export alias `aspl::libASPL` or document target name `libASPL`

4. Include from root `CMakeLists.txt` before `Driver` subdirectory (add `add_subdirectory(Driver)` when Driver exists in plan 03 — include LibASPL.cmake now even if Driver added in 04-03).
  </action>
  <verify>
    <automated>test -d third_party/libASPL && git -C third_party/libASPL describe --tags | grep -q v3.1.2; cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug 2>&1 | tail -3</automated>
  </verify>
  <done>Submodule at v3.1.2; cmake configure succeeds with LibASPL.cmake included.</done>
</task>

</tasks>

<verification>
- `git submodule status third_party/libASPL` shows commit at v3.1.2
- `cmake -S . -B build` completes without libASPL errors
</verification>

<success_criteria>
- Reproducible libASPL pin documented in third_party/README.md
</success_criteria>
