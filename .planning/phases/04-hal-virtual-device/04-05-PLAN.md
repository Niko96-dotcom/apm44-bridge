---
phase: 04-hal-virtual-device
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - scripts/install-driver.sh
  - scripts/verify-hal-driver.sh
  - docs/hal-driver.md
  - README.md
  - .planning/phases/04-hal-virtual-device/04-VERIFICATION.md
autonomous: true
requirements:
  - DEV-01
  - DEV-03
must_haves:
  truths:
    - "Documented install path to /Library/Audio/Plug-Ins/HAL/"
    - "verify-hal-driver.sh checks bundle exists and prints load instructions"
  artifacts:
    - path: scripts/install-driver.sh
      provides: "Dev driver install"
    - path: docs/hal-driver.md
      provides: "DAW routing + virtual-device daemon flow"
  key_links:
    - from: docs/hal-driver.md
      to: scripts/install-driver.sh
      pattern: "install-driver"
---

<objective>
Ship install/verify scripts and **hal-driver.md**; write **04-VERIFICATION.md** with honest gaps for unsigned HAL load and manual DAW QA.
</objective>

<context>
@.planning/phases/04-hal-virtual-device/04-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: install-driver.sh + verify-hal-driver.sh</name>
  <files>scripts/install-driver.sh, scripts/verify-hal-driver.sh</files>
  <action>
`install-driver.sh`:
- Requires built `build/Driver/APM44Bridge.driver`
- `sudo cp -R` to `/Library/Audio/Plug-Ins/HAL/`, `chown root:wheel`
- Print `sudo launchctl kickstart -k system/com.apple.audio.coreaudiod` reminder

`verify-hal-driver.sh`:
- Check bundle structure, plist keys, embedded UID string
- Optional: `system_profiler SPAudioDataType` grep APM44 (non-fatal if unsigned)

Executable bits set.
  </action>
  <verify>
    <automated>bash -n scripts/install-driver.sh && bash -n scripts/verify-hal-driver.sh && scripts/verify-hal-driver.sh 2>&1 | head -20</automated>
  </verify>
  <done>Scripts pass shellcheck/bash -n; verify runs (may warn unsigned).</done>
</task>

<task type="auto">
  <name>Task 2: Documentation + README link</name>
  <files>docs/hal-driver.md, README.md</files>
  <action>
Document:
1. Build driver + daemon
2. Install HAL plug-in
3. DAW → APM44 Bridge @ 44.1 kHz
4. `apm44-bridge --virtual-device --output-device <AirPods UID>`
5. Signing/notarization deferred Phase 5

Note BlackHole-free goal and current skeleton limitations.
  </action>
  <verify>
    <automated>test -f docs/hal-driver.md && rg -q 'virtual-device' docs/hal-driver.md</automated>
  </verify>
  <done>Docs present and linked from README.</done>
</task>

</tasks>

<verification>
- Scripts exist and are documented
</verification>

<success_criteria>
- Operator can follow docs for dev install attempt without reading source
</success_criteria>
