---
phase: 04-hal-virtual-device
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - BridgeDaemon/src/CliOptions.h
  - BridgeDaemon/src/CliOptions.cpp
  - BridgeDaemon/src/engine/VirtualDeviceFeed.h
  - BridgeDaemon/src/engine/VirtualDeviceFeed.cpp
  - BridgeDaemon/src/engine/BridgeEngine.h
  - BridgeDaemon/src/engine/BridgeEngine.cpp
  - BridgeDaemon/src/main.cpp
  - tests/test_virtual_device_feed.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - DRV-03
  - DEV-03
must_haves:
  truths:
    - "apm44-bridge --virtual-device runs without BlackHole input HAL client"
    - "Audio from shm ring feeds the existing SRC/drift pipeline to AirPods output"
  artifacts:
    - path: BridgeDaemon/src/engine/VirtualDeviceFeed.cpp
      provides: "Shm consumer → PlanarRingBuffer"
    - path: BridgeDaemon/src/CliOptions.cpp
      provides: "--virtual-device flag"
  key_links:
    - from: BridgeEngine.cpp
      to: VirtualDeviceFeed
      pattern: "virtualDevice"
---

<objective>
Add daemon **`--virtual-device`** mode: consume driver shm ring instead of BlackHole input IOProc (DRV-03, DEV-03 partial — DAW routing still needs driver installed).
</objective>

<context>
@.planning/phases/04-hal-virtual-device/04-CONTEXT.md
@BridgeDaemon/src/engine/BridgeEngine.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: VirtualDeviceFeed + CLI flag</name>
  <files>BridgeDaemon/src/engine/VirtualDeviceFeed.h, BridgeDaemon/src/engine/VirtualDeviceFeed.cpp, BridgeDaemon/src/CliOptions.h, BridgeDaemon/src/CliOptions.cpp, BridgeDaemon/CMakeLists.txt</files>
  <action>
`CliOptions::virtualDevice` bool, `--virtual-device` in usage.

`VirtualDeviceFeed`: opens `MmapShmRing` as Consumer; `drainTo(PlanarRingBuffer&, maxFrames)` called from RT path.

Wait for `daemon_ready` / header valid before draining (non-RT prepare ok).
  </action>
  <verify>
    <automated>cmake --build build --target apm44-bridge && build/BridgeDaemon/apm44-bridge --help | grep -q virtual-device</automated>
  </verify>
  <done>Flag parses; feed compiles.</done>
</task>

<task type="auto">
  <name>Task 2: BridgeEngine virtual mode (output-only IOProc)</name>
  <files>BridgeDaemon/src/engine/BridgeEngine.h, BridgeDaemon/src/engine/BridgeEngine.cpp, BridgeDaemon/src/main.cpp, tests/test_virtual_device_feed.cpp</files>
  <action>
When `virtualDevice`:
- `prepare()` skips input device negotiation; only output AirPods pair required
- `start()` registers **output IOProc only**
- At start of `onOutput`, call `virtualFeed_.drainTo(ring_, framesNeeded)` before existing pop/SRC logic OR replace input path: drain shm → ring then process as today

`main.cpp`: if `--virtual-device`, resolve output only; warn if shm not open.

Offline test: producer pushes in test setup, virtual feed drains into ring, assert fill > 0.
  </action>
  <verify>
    <automated>ctest --test-dir build -R virtual_device --output-on-failure 2>/dev/null || cmake --build build && ctest --test-dir build -R virtual_device --output-on-failure</automated>
  </verify>
  <done>Virtual mode builds; test passes without hardware.</done>
</task>

</tasks>

<verification>
- `apm44-bridge --help` documents `--virtual-device`
- Engine starts output-only path without input AudioDeviceStart
</verification>

<success_criteria>
- End-to-end software path driver shm → daemon ring → SRC ready for manual HAL test
</success_criteria>
