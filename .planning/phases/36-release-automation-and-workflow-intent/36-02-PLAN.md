---
phase: 36-release-automation-and-workflow-intent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/sign-notarize.yml
  - docs/release.md
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - GHA-01
  - GHA-02
must_haves:
  truths:
    - "`sign-notarize.yml` identifies itself as maintainer signing/notary evidence."
    - "Release docs say `sign-notarize.yml` does not publish the public DMG."
    - "Release-script tests guard the workflow intent wording."
---

<objective>
Clarify GitHub signing workflow artifact intent in workflow comments and release docs.
</objective>

<tasks>

<task type="auto">
  <name>Document GitHub workflow as evidence-only</name>
  <read_first>.github/workflows/sign-notarize.yml, docs/release.md, tests/test_release_scripts.sh</read_first>
  <action>Update `.github/workflows/sign-notarize.yml` name/comments to say it is maintainer signing/notary evidence and does not publish the public DMG. Update `docs/release.md` GitHub Actions trust decision to say `sign-notarize.yml` produces no public release artifact unless upload is added later. Extend release-script tests to assert this wording.</action>
  <acceptance_criteria>
    - `.github/workflows/sign-notarize.yml` contains `maintainer signing/notary evidence`.
    - `docs/release.md` contains `does not publish the public DMG`.
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
