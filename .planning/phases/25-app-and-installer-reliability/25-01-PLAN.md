---
phase: 25-app-and-installer-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App/APM44Bridge/DeviceCatalog.swift
  - App/APM44Bridge/BridgeProcessManager.swift
  - tests/test_device_catalog.swift
  - tests/test_bridge_process_manager.swift
autonomous: true
requirements:
  - APP-06
  - APP-07
must_haves:
  truths:
    - "`DeviceCatalog.refresh()` cannot block on an unread helper stderr pipe."
    - "Bridge start resets `latestMetrics`, `lastMetricsAt`, and `metricsStale` together."
    - "Idle transition resets `latestMetrics`, `lastMetricsAt`, and `metricsStale` together."
  artifacts:
    - path: App/APM44Bridge/DeviceCatalog.swift
      provides: "Non-blocking helper stderr behavior"
      contains: "FileHandle.nullDevice"
    - path: App/APM44Bridge/BridgeProcessManager.swift
      provides: "Shared metrics lifecycle reset helper"
      contains: "resetMetricsState"
---

<objective>
Harden DeviceCatalog stderr handling and metrics timestamp reset.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/25-app-and-installer-reliability/25-CONTEXT.md
@App/APM44Bridge/DeviceCatalog.swift
@App/APM44Bridge/BridgeProcessManager.swift
@tests/test_device_catalog.swift
@tests/test_bridge_process_manager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Avoid unread helper stderr pipe in DeviceCatalog refresh</name>
  <files>App/APM44Bridge/DeviceCatalog.swift, tests/test_device_catalog.swift</files>
  <acceptance_criteria>
    - `DeviceCatalog.refresh` does not assign an unread `Pipe()` to `standardError`.
    - Swift test coverage guards the stderr behavior.
  </acceptance_criteria>
  <action>
    Send helper stderr to `FileHandle.nullDevice` and add a source-level Swift test that fails if the unread pipe returns.
  </action>
</task>

<task type="auto">
  <name>Task 2: Reset metrics payload and timestamp together</name>
  <files>App/APM44Bridge/BridgeProcessManager.swift, tests/test_bridge_process_manager.swift</files>
  <acceptance_criteria>
    - Start clears `latestMetrics`, `lastMetricsAt`, and `metricsStale` together.
    - Transition to idle clears `latestMetrics`, `lastMetricsAt`, and `metricsStale` together.
    - Swift unit tests cover both transitions.
  </acceptance_criteria>
  <action>
    Introduce `resetMetricsState()` and call it from start and idle transition paths. Add internal testing hooks for metrics ingestion and timestamp visibility.
  </action>
</task>

</tasks>

<verification>
```bash
./script/dev.sh check
```
</verification>
