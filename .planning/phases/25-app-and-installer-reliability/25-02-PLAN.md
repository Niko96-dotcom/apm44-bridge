---
phase: 25-app-and-installer-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - scripts/build-release-dmg.sh
  - tests/test_release_scripts.sh
autonomous: true
requirements:
  - DIST-05
must_haves:
  truths:
    - "Generated DMG command removes existing `/Applications/APM44 Bridge.app`."
    - "Generated DMG command copies the app with privileged `ditto`."
    - "Generated DMG command sets app ownership to `root:wheel`."
  artifacts:
    - path: scripts/build-release-dmg.sh
      provides: "Deterministic app bundle install command"
      contains: "sudo ditto"
    - path: tests/test_release_scripts.sh
      provides: "Release-script regression for installer command"
      contains: "DIST-05"
---

<objective>
Make the DMG command installer app copy deterministic.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/25-app-and-installer-reliability/25-CONTEXT.md
@scripts/build-release-dmg.sh
@tests/test_release_scripts.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden generated app install command</name>
  <files>scripts/build-release-dmg.sh</files>
  <acceptance_criteria>
    - Generated command removes `/Applications/APM44 Bridge.app`.
    - Generated command uses `sudo ditto` for app copy.
    - Generated command runs `sudo chown -R root:wheel` on the installed app.
  </acceptance_criteria>
  <action>
    Replace the plain `cp -R` app install with privileged remove/copy/ownership commands.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add release-script regression guard</name>
  <files>tests/test_release_scripts.sh</files>
  <acceptance_criteria>
    - `tests/test_release_scripts.sh` fails if the deterministic installer command lines are missing.
    - `bash tests/test_release_scripts.sh` exits 0.
  </acceptance_criteria>
  <action>
    Add a DIST-05 source guard for the generated command heredoc.
  </action>
</task>

</tasks>

<verification>
```bash
bash tests/test_release_scripts.sh
```
</verification>
