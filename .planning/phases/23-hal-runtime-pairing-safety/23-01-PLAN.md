---
phase: 23-hal-runtime-pairing-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Driver/src/ShmIoHandler.cpp
  - Driver/src/ShmIoHandler.h
  - tests/test_shm_io_handler.cpp
autonomous: true
requirements:
  - HAL-01
  - HAL-02
must_haves:
  truths:
    - "Mono lane left/right blocks with unrelated logical timestamps are not paired into stereo shm output."
    - "Rollover pairing uses a named logical-time predicate based on zeroTimestamp + timestamp."
    - "Existing normal same-timestamp mono pairing remains intact."
  artifacts:
    - path: Driver/src/ShmIoHandler.cpp
      provides: "Fail-closed mono lane matching"
      contains: "laneTimesMatch"
    - path: tests/test_shm_io_handler.cpp
      provides: "Catch2 coverage for mismatch rejection and rollover acceptance"
      contains: "rejects unrelated mono lane timestamp mismatches"
---

<objective>
Make HAL mono-lane timestamp pairing explicit and fail closed.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-hal-runtime-pairing-safety/23-CONTEXT.md
@Driver/src/ShmIoHandler.cpp
@Driver/src/ShmIoHandler.h
@tests/test_shm_io_handler.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store and compare logical mono lane timestamps</name>
  <files>Driver/src/ShmIoHandler.cpp, Driver/src/ShmIoHandler.h</files>
  <acceptance_criteria>
    - `Driver/src/ShmIoHandler.cpp` contains a named `laneTimesMatch` predicate.
    - Mono lane enqueue receives `zeroTimestamp + timestamp` logical time.
    - `flushPendingLanes` does not call `pushLanePair` unless `laneTimesMatch` returns true.
  </acceptance_criteria>
  <action>
    Replace raw timestamp matching with logical lane time matching. Preserve queue search for future matching blocks, but when no match exists, drop the older unmatched lane head instead of pairing arbitrary mismatches.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add HAL mono-lane timestamp regressions</name>
  <files>tests/test_shm_io_handler.cpp</files>
  <acceptance_criteria>
    - A mismatch test proves unrelated left/right timestamps do not produce shm frames.
    - Existing normal matching and rollover matching tests pass.
    - `cmake --build build --target test_shm_io_handler && ctest --test-dir build -R test_shm_io_handler --output-on-failure` exits 0.
  </acceptance_criteria>
  <action>
    Add a Catch2 regression using isolated ring names and existing `Apm44OutputStream` setup to prove unrelated timestamps are rejected. Keep the rollover test as the positive case for the named logical-time predicate.
  </action>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_shm_io_handler
ctest --test-dir build -R test_shm_io_handler --output-on-failure
```
</verification>
