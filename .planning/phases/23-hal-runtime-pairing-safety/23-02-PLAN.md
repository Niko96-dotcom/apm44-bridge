---
phase: 23-hal-runtime-pairing-safety
plan: 02
type: execute
wave: 1
depends_on:
  - 23-01
files_modified:
  - Driver/src/ShmIoHandler.cpp
  - tests/test_shm_io_handler.cpp
autonomous: true
requirements:
  - HAL-03
  - HAL-04
must_haves:
  truths:
    - "Mixed-output callbacks after OnStopIO return before stream processing and shm writes."
    - "HAL shm push comment describes bounded write/drop-incoming-tail behavior."
    - "Targeted Catch2 regression proves stopped IO writes no frames."
  artifacts:
    - path: Driver/src/ShmIoHandler.cpp
      provides: "Stopped IO guard and accurate drop-policy comment"
      contains: "drops incoming tail frames"
    - path: tests/test_shm_io_handler.cpp
      provides: "Stopped IO regression"
      contains: "ignores mixed output after IO stops"
---

<objective>
Guard stopped IO callbacks and correct HAL shared-memory drop-policy documentation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-hal-runtime-pairing-safety/23-CONTEXT.md
@Driver/src/ShmIoHandler.cpp
@tests/test_shm_io_handler.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return early when IO is stopped</name>
  <files>Driver/src/ShmIoHandler.cpp</files>
  <acceptance_criteria>
    - `OnProcessMixedOutput` checks `ioRunning_` before applying stream processing.
    - Stopped callbacks do not call `pushInterleaved` or `pushMonoLane`.
  </acceptance_criteria>
  <action>
    Add `!ioRunning_` to the early return guard in `OnProcessMixedOutput`, before `stream->ApplyProcessing`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Correct comment and add stopped-IO regression</name>
  <files>Driver/src/ShmIoHandler.cpp, tests/test_shm_io_handler.cpp</files>
  <acceptance_criteria>
    - The HAL shm push comment says incoming tail frames are dropped when the bounded ring is full.
    - A Catch2 test starts IO, stops IO, sends mixed output, and verifies no frames are available to the consumer.
    - `cmake --build build --target test_shm_io_handler && ctest --test-dir build -R test_shm_io_handler --output-on-failure` exits 0.
  </acceptance_criteria>
  <action>
    Update the inaccurate oldest-frame comment and add the stopped-IO regression beside existing ShmIoHandler tests.
  </action>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_shm_io_handler
ctest --test-dir build -R test_shm_io_handler --output-on-failure
```
</verification>
