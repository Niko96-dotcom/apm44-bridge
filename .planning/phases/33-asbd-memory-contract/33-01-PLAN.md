---
phase: 33-asbd-memory-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/src/AudioFormats.cpp
autonomous: true
requirements:
  - ASBD-01
  - ASBD-02
  - ASBD-03
must_haves:
  truths:
    - "`AsbdMatchesFloat32Stereo` rejects `mFramesPerPacket != 1` before accepting a format."
    - "Interleaved stereo Float32 requires `kAudioFormatFlagIsPacked` and two-float packet/frame byte sizes."
    - "Non-interleaved stereo Float32 requires one-float packet/frame byte sizes per channel buffer."
---

<objective>
Tighten `AsbdMatchesFloat32Stereo` so accepted ASBDs exactly match the IOProc Float32 stereo memory layouts.
</objective>

<tasks>

<task type="auto">
  <name>Enforce exact packet and frame layout</name>
  <read_first>Shared/src/AudioFormats.cpp</read_first>
  <action>In `AsbdMatchesFloat32Stereo`, keep the existing format ID, Float32, stereo, bits-per-channel, and sample-rate checks. Add a required `mFramesPerPacket == 1` check. For non-interleaved ASBDs require `mBytesPerPacket == sizeof(float)` and `mBytesPerFrame == sizeof(float)`. For interleaved ASBDs require `kAudioFormatFlagIsPacked`, `mBytesPerPacket == sizeof(float) * 2`, and `mBytesPerFrame == sizeof(float) * 2`.</action>
  <acceptance_criteria>
    - `Shared/src/AudioFormats.cpp` contains `asbd.mFramesPerPacket != 1`.
    - `Shared/src/AudioFormats.cpp` contains `kAudioFormatFlagIsNonInterleaved`.
    - `Shared/src/AudioFormats.cpp` contains `sizeof(float) * 2`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_audio_formats
ctest --test-dir build --output-on-failure -R test_audio_formats
```
</verification>
