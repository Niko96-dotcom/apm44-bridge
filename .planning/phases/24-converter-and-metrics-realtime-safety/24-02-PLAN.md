---
phase: 24-converter-and-metrics-realtime-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - BridgeDaemon/src/engine/MetricsPublisher.h
  - tests/test_bridge_metrics_json.cpp
autonomous: true
requirements:
  - METR-04
must_haves:
  truths:
    - "`MetricsPublisherState` contains no `std::atomic<double>` fields."
    - "Metrics floating fields round-trip through `std::atomic<uint64_t>` bit-packed storage."
    - "Existing metrics publisher API remains stable."
  artifacts:
    - path: BridgeDaemon/src/engine/MetricsPublisher.h
      provides: "Lock-free bit-packed metrics floating field storage"
      contains: "std::atomic<uint64_t> fillMsBits"
    - path: tests/test_bridge_metrics_json.cpp
      provides: "Metrics storage invariant regression"
      contains: "MetricsPublisherPackedFloatingFieldsRoundTrip"
---

<objective>
Replace metrics double atomics with lock-free packed storage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/24-converter-and-metrics-realtime-safety/24-CONTEXT.md
@BridgeDaemon/src/engine/MetricsPublisher.h
@tests/test_bridge_metrics_json.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bit-pack floating metrics fields into uint64 atomics</name>
  <files>BridgeDaemon/src/engine/MetricsPublisher.h</files>
  <acceptance_criteria>
    - `MetricsPublisherState` contains no `std::atomic<double>`.
    - Floating fields are stored in `std::atomic<uint64_t>` members.
    - `PublishMetrics` and `ReadMetrics` preserve exact double values via bit casts.
  </acceptance_criteria>
  <action>
    Introduce small pack/unpack helpers using `std::bit_cast<uint64_t>` and `std::bit_cast<double>`, then store/load `fillMs`, `smoothedRatio`, and `ppm` through uint64 atomics.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add metrics storage invariant tests</name>
  <files>tests/test_bridge_metrics_json.cpp</files>
  <acceptance_criteria>
    - A test publishes non-trivial floating values and reads back exact values.
    - A source guard asserts `std::atomic<double>` is absent from `MetricsPublisher.h`.
    - `cmake --build build --target test_bridge_metrics_json && ctest --test-dir build -R test_bridge_metrics_json --output-on-failure` exits 0.
  </acceptance_criteria>
  <action>
    Extend the existing metrics publisher test file with bit-packed round-trip assertions and a source guard for the forbidden atomic type.
  </action>
</task>

</tasks>

<verification>
```bash
cmake --build build --target test_bridge_metrics_json
ctest --test-dir build -R test_bridge_metrics_json --output-on-failure
```
</verification>
