---
phase: 24-converter-and-metrics-realtime-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BridgeDaemon/src/CliOptions.cpp
  - BridgeDaemon/src/CliOptions.h
  - BridgeDaemon/src/engine/BridgeEngine.cpp
  - BridgeDaemon/src/engine/BridgeEngine.h
  - BridgeDaemon/CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/test_bridge_metrics_json.cpp
  - docs/soak-test.md
autonomous: true
requirements:
  - CONV-01
must_haves:
  truths:
    - "`--legacy-converter` is absent from CLI help, parsing, docs, and tests."
    - "`AudioConverterSrc` is absent from CMake build inputs and runtime engine options."
    - "Bridge runtime always uses the supported libsamplerate SRC path."
  artifacts:
    - path: tests/test_bridge_metrics_json.cpp
      provides: "Source guard for removed converter path"
      contains: "LegacyConverterPathRemovedFromPublicRuntime"
---

<objective>
Remove the public legacy AudioToolbox converter path.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/24-converter-and-metrics-realtime-safety/24-CONTEXT.md
@BridgeDaemon/src/CliOptions.cpp
@BridgeDaemon/src/engine/BridgeEngine.cpp
@BridgeDaemon/CMakeLists.txt
@tests/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy converter from public runtime surfaces</name>
  <files>BridgeDaemon/src/CliOptions.cpp, BridgeDaemon/src/CliOptions.h, BridgeDaemon/src/engine/BridgeEngine.cpp, BridgeDaemon/src/engine/BridgeEngine.h, docs/soak-test.md</files>
  <acceptance_criteria>
    - `rg -- '--legacy-converter' BridgeDaemon docs tests README.md scripts .github` returns no matches.
    - `BridgeEngineOptions` contains no legacy converter option.
    - `BridgeEngine` always prepares and processes through `LibSamplerateSrc`.
  </acceptance_criteria>
  <action>
    Remove the CLI flag, option field, engine option field, runtime branch, startup log field, and documentation snippet for the legacy converter.
  </action>
</task>

<task type="auto">
  <name>Task 2: Remove converter build inputs and add guard coverage</name>
  <files>BridgeDaemon/CMakeLists.txt, tests/CMakeLists.txt, tests/test_bridge_metrics_json.cpp</files>
  <acceptance_criteria>
    - `AudioConverterSrc.cpp` is not listed in CMake inputs.
    - `test_audio_converter_src` is not listed in `tests/CMakeLists.txt`.
    - Native test guard fails if legacy converter public/runtime references return.
  </acceptance_criteria>
  <action>
    Remove converter source/test targets from CMake, delete the obsolete source/test files, and add a source-level Catch2 guard for the removed public path.
  </action>
</task>

</tasks>

<verification>
```bash
cmake --build build --target apm44-bridge test_bridge_metrics_json
ctest --test-dir build -R test_bridge_metrics_json --output-on-failure
```
</verification>
