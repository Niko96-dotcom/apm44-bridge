---
phase: 02-production-src-drift-engine
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - BridgeDaemon/src/engine/LibSamplerateSrc.h
  - BridgeDaemon/src/engine/LibSamplerateSrc.cpp
  - BridgeDaemon/CMakeLists.txt
  - tests/test_lib_samplerate_src.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements:
  - ENG-02
must_haves:
  truths:
    - "Persistent SRC_STATE per stream; src_process in streaming mode with variable src_ratio"
    - "Default quality SRC_SINC_MEDIUM_QUALITY; CLI mapping prepared for medium/high/best"
    - "Nominal ratio 48000/44100 applied when drift ratio not overridden"
  artifacts:
    - path: BridgeDaemon/src/engine/LibSamplerateSrc.cpp
      provides: "libsamplerate wrapper for bridge"
      exports: ["LibSamplerateSrc"]
  key_links:
    - from: LibSamplerateSrc.cpp
      to: src_process
      via: "persistent SRC_STATE"
      pattern: "src_process"
---

## Phase Goal

**As a** producer monitoring through AirPods Max USB-C, **I want** the bridge to hold stable 44.1→48 conversion with drift correction over long sessions, **so that** I hear uninterrupted audio without crackle or growing latency.

<objective>
Add **LibSamplerateSrc** — RT-safe wrapper around persistent `SRC_STATE` with variable `src_ratio` (ENG-02), replacing the role of `AudioConverterSrc` in the output path.

Purpose: Production streaming SRC with ratio updates for drift (wired in plan 05).
Output: Unit-tested converter class linked against vendored libsamplerate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-production-src-drift-engine/02-CONTEXT.md
@BridgeDaemon/src/engine/AudioConverterSrc.h
@BridgeDaemon/src/engine/AudioConverterSrc.cpp
@.planning/phases/02-production-src-drift-engine/02-01-SUMMARY.md

<interfaces>
Mirror `AudioConverterSrc` surface for drop-in:
```cpp
class LibSamplerateSrc {
 public:
  enum class Quality { Medium, High, Best };
  bool prepare(Quality quality = Quality::Medium);
  void setRatio(double ratio);  // calls src_set_ratio when change exceeds epsilon
  bool process(const float* const inputChannels[2], std::size_t inputFrames,
               float* const outputChannels[2], std::size_t outputCapacity,
               std::size_t& outputFramesWritten);
  double nominalRatio() const;
 private:
  SRC_STATE* state_ = nullptr;
  // preallocated SRC_DATA / scratch — no malloc in process()
};
```
CONTEXT: `src_new(SRC_SINC_MEDIUM_QUALITY, 2, ...)` default; map Quality → SRC_SINC_* constants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LibSamplerateSrc RT wrapper</name>
  <files>BridgeDaemon/src/engine/LibSamplerateSrc.h, BridgeDaemon/src/engine/LibSamplerateSrc.cpp, BridgeDaemon/CMakeLists.txt</files>
  <behavior>
    - `prepare()` creates one SRC_STATE; second `prepare()` is idempotent or returns false.
    - 147 input frames at ratio 48000/44100 yields ~160 output frames (±5%).
    - Changing ratio via `setRatio` mid-stream does not reset state with `src_simple`.
  </behavior>
  <action>
Implement `LibSamplerateSrc` per CONTEXT **libsamplerate integration**:

1. `prepare(Quality)`: `src_new` with `SRC_SINC_MEDIUM_QUALITY` (High/Best mapped per STACK.md); channels=2; allocate internal planar scratch in `prepare()` only.

2. `setRatio(double)`: use `src_set_ratio` when delta exceeds 1e-9; store last ratio for `nominalRatio()` reporting.

3. `process(...)`: fill `SRC_DATA` for each channel (or interleaved per libsamplerate docs — prefer documented planar pattern from api_full); call `src_process`; return false on error (caller silences).

4. **Forbidden in `process`:** malloc, free, logging, `src_simple`.

5. Link `apm44-bridge` target to `samplerate` in CMake; add `.cpp` to target sources (may remain unused until plan 05 — compile with `-Wno-unused` not needed if object linked).

**Keep `AudioConverterSrc` untouched** in this plan — legacy path removed in plan 05.
  </action>
  <verify>
    <automated>cmake --build build; ctest --test-dir build -R lib_samplerate_src --output-on-failure</automated>
  </verify>
  <done>Wrapper compiles; unit tests pass; single `src_new` per instance (grep BridgeDaemon/src/engine/LibSamplerateSrc.cpp).</done>
</task>

<task type="auto">
  <name>Task 2: Quality enum + ratio drift simulation test</name>
  <files>tests/test_lib_samplerate_src.cpp, tests/CMakeLists.txt</files>
  <action>
Add `test_lib_samplerate_src` linking `LibSamplerateSrc.cpp` + samplerate.

Tests:
- 440 Hz sine 44.1→48 length tolerance (reuse pattern from `test_audio_converter_src.cpp`).
- **Ratio drift simulation:** feed 100 blocks while stepping `setRatio` by +100 ppm then -100 ppm; assert no NaN output and output frames positive each block.

Map `Quality::Best` → `SRC_SINC_BEST_QUALITY` for future Phase 3 presets (CONTEXT `--src-quality`).

**Per ENG-02:** variable-ratio streaming, not fixed-only.
  </action>
  <verify>
    <automated>ctest --test-dir build -R lib_samplerate_src --output-on-failure</automated>
  </verify>
  <done>Drift simulation test passes; quality enum maps to three SRC_SINC constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RT IOProc | `LibSamplerateSrc::process` on output callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service | LibSamplerateSrc | mitigate | Cap `inputFrames` and `outputCapacity` to scratch size; return false if exceeded |
</threat_model>

<verification>
- CTest lib_samplerate_src green
- No `src_simple` in bridge engine tree
</verification>

<success_criteria>
- LibSamplerateSrc ready for BridgeEngine integration with variable ratio
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-src-drift-engine/02-04-SUMMARY.md`
</output>
