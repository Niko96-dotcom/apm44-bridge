---
phase: 02-production-src-drift-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - third_party/libsamplerate/.gitkeep
  - cmake/Libsamplerate.cmake
  - BridgeDaemon/CMakeLists.txt
  - tests/CMakeLists.txt
  - tests/test_libsamplerate_smoke.cpp
autonomous: true
requirements:
  - ENG-02
must_haves:
  truths:
    - "apm44-bridge links libsamplerate 0.2.2 statically with reproducible pin"
    - "CI can compile and run a smoke test calling src_new/src_process without hardware"
  artifacts:
    - path: cmake/Libsamplerate.cmake
      provides: "Pinned libsamplerate static target"
      exports: ["samplerate"]
    - path: tests/test_libsamplerate_smoke.cpp
      provides: "Offline SRC smoke"
  key_links:
    - from: BridgeDaemon/CMakeLists.txt
      to: samplerate
      via: "target_link_libraries"
      pattern: "samplerate"
---

## Phase Goal

**As a** producer monitoring through AirPods Max USB-C, **I want** the bridge to hold stable 44.1→48 conversion with drift correction over long sessions, **so that** I hear uninterrupted audio without crackle or growing latency.

<objective>
Vendor **libsamplerate 0.2.2** into the CMake build and prove the toolchain can create and run a minimal `src_process` offline — foundation for production SRC (ENG-02).

Purpose: Reproducible static link per STACK.md and CONTEXT locked decision; no Homebrew-only runtime dep.
Output: `samplerate` CMake target, smoke Catch2 test in CTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-production-src-drift-engine/02-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@CMakeLists.txt
@BridgeDaemon/CMakeLists.txt
@tests/CMakeLists.txt

<interfaces>
From STACK.md / CONTEXT:
- Pin tag **0.2.2** at `third_party/libsamplerate` (git submodule preferred; FetchContent with `GIT_TAG 0.2.2` acceptable if submodule init documented in `docs/build.md` one line).
- Build static library; BSD-2-Clause — record in `third_party/README.md` if created.
- API: `src_new`, `src_process`, `src_set_ratio`, `src_delete` — never `src_simple()` in streaming path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor libsamplerate 0.2.2 and CMake target</name>
  <files>third_party/libsamplerate, cmake/Libsamplerate.cmake, CMakeLists.txt</files>
  <action>
Per CONTEXT **libsamplerate integration** (vendor 0.2.2):

1. Add git submodule `https://github.com/libsndfile/libsamplerate.git` at `third_party/libsamplerate`, checked out at tag **0.2.2**. Add `third_party/README.md` noting version + license.

2. Create `cmake/Libsamplerate.cmake` that `add_subdirectory(third_party/libsamplerate EXCLUDE_FROM_ALL)` (or equivalent) and exposes alias target `samplerate::samplerate` (or `samplerate`) as **STATIC** only.

3. Include from root `CMakeLists.txt` **before** `add_subdirectory(BridgeDaemon)`.

4. Disable libsamplerate tests/examples in cache vars if supported (`LIBSAMPLERATE_EXAMPLES OFF`, etc.) to keep CI fast.

5. Do **not** link libsamplerate into `apm44_shared` yet — BridgeDaemon and tests link explicitly in later plans.

**Avoid:** Homebrew-only link for release binaries; floating `main` branch.
  </action>
  <verify>
    <automated>git -C third_party/libsamplerate describe --tags 2>/dev/null | grep -q 0.2.2; cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug; cmake --build build --target samplerate</automated>
  </verify>
  <done>Submodule or FetchContent pin resolves to 0.2.2; `cmake --build build` produces static libsamplerate artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline libsamplerate smoke test</name>
  <files>tests/test_libsamplerate_smoke.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - Create `SRC_STATE` with `SRC_SINC_MEDIUM_QUALITY`, 2 channels.
    - Feed 147 frames of stereo sine @ 44100; `src_ratio = 48000/44100`; `src_process` produces output length within 5% of 160 frames.
    - `src_delete` without leak (ASAN optional, not required in CI).
  </behavior>
  <action>
Add `test_libsamplerate_smoke` via `apm44_add_test`, linking `PRIVATE samplerate` (or `samplerate::samplerate`).

Use preallocated `SRC_DATA` arrays on stack — no malloc in test hot loop.

Document in test comment: nominal ratio 160/147 for 44.1→48.

**Per ENG-02:** proves streaming API works before `LibSamplerateSrc` wrapper.
  </action>
  <verify>
    <automated>cmake --build build; ctest --test-dir build -R libsamplerate_smoke --output-on-failure</automated>
  </verify>
  <done>CTest passes smoke test; `rg -v '^#' tests/test_libsamplerate_smoke.cpp | rg -c src_process` is ≥ 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party C | libsamplerate compiled into daemon — supply-chain integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | third_party/libsamplerate | mitigate | Pin git tag 0.2.2; submodule SHA recorded in plan SUMMARY |
| T-02-02 | Denial of Service | Build | accept | Dev-only; malformed input not in scope for vendor step |
</threat_model>

<verification>
- `ctest -R libsamplerate` passes on clean clone after `git submodule update --init`
- No GPL sources added (libsamplerate is BSD-2-Clause)
</verification>

<success_criteria>
- Static libsamplerate 0.2.2 builds on macOS 14+ via project CMake
- Smoke test validates `src_process` nominal ratio path
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-src-drift-engine/02-01-SUMMARY.md`
</output>
