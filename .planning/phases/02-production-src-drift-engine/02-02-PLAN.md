---
phase: 02-production-src-drift-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Shared/include/apm44/PlanarRingBuffer.h
  - Shared/src/PlanarRingBuffer.cpp
  - tests/test_planar_ring_buffer.cpp
autonomous: true
requirements:
  - ENG-03
must_haves:
  truths:
    - "Ring capacity is power-of-two; SPSC indices use acquire/release ordering"
    - "Caller can read fill level in frames and milliseconds at 44.1 kHz"
    - "Target fill default 15 ms is computable from sample rate without RT allocation"
  artifacts:
    - path: Shared/include/apm44/PlanarRingBuffer.h
      provides: "SPSC planar ring with fill metrics"
      exports: ["PlanarRingBuffer"]
  key_links:
    - from: PlanarRingBuffer.cpp
      to: atomic read/write indices
      via: "mask (index & capacity-1)"
      pattern: "capacityFrames_ - 1"
---

## Phase Goal

**As a** producer monitoring through AirPods Max USB-C, **I want** the bridge to hold stable 44.1→48 conversion with drift correction over long sessions, **so that** I hear uninterrupted audio without crackle or growing latency.

<objective>
Upgrade `PlanarRingBuffer` to production SPSC semantics with power-of-two capacity and fill reporting for the drift controller (ENG-03).

Purpose: Drift PI loop needs accurate fill vs target in samples @ 44.1 kHz; RT path stays malloc-free.
Output: Enhanced ring API + expanded unit tests; no BridgeEngine wiring in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-production-src-drift-engine/02-CONTEXT.md
@Shared/include/apm44/PlanarRingBuffer.h
@Shared/src/PlanarRingBuffer.cpp
@tests/test_planar_ring_buffer.cpp

<interfaces>
Current API (Phase 1):
```cpp
void prepare(std::size_t capacityFrames);
std::size_t push(const float* const channelData[2], std::size_t frameCount);
std::size_t pop(float* const channelData[2], std::size_t frameCount);
std::size_t availableToRead() const;
std::size_t availableToWrite() const;
```
CONTEXT: default target fill **15 ms** @ 44100 ≈ 662 samples; ring sized to hold ≥ 2× target + block size.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Power-of-two SPSC ring with mask indexing</name>
  <files>Shared/include/apm44/PlanarRingBuffer.h, Shared/src/PlanarRingBuffer.cpp, tests/test_planar_ring_buffer.cpp</files>
  <behavior>
    - `prepare(100)` rounds capacity up to next power of two (128); push/pop preserve sample order.
    - SPSC reserve-one-slot: max usable frames = capacity - 1 (update tests if capacity semantics change from Phase 1).
    - Concurrent single-producer/single-consumer stress: 10k push/pop alternation without corruption (single-threaded simulation acceptable).
  </behavior>
  <action>
Per CONTEXT **Ring buffer & timing**:

1. In `prepare(requestedFrames)`, set `capacityFrames_` to smallest power of two ≥ max(requestedFrames, 2); allocate `capacityFrames_ * kChannels` floats once.

2. Replace `% capacityFrames_` with `index & (capacityFrames_ - 1)` in push/pop.

3. Keep **lock-free SPSC**: producer updates `writeIndex_` with release; consumer loads write with acquire; symmetric for read index.

4. Add `std::size_t capacityFrames() const` (unchanged) and `bool isPowerOfTwoCapacity() const` for tests only if needed.

5. **No mutex**; no logging in ring methods.

**Discretion:** exact memory barriers — follow existing acquire/release pattern from Phase 1, tightened for SPSC.

Update `test_planar_ring_buffer` for new capacity rounding (document expected usable capacity in test name).
  </action>
  <verify>
    <automated>cmake --build build &amp;&amp; ctest --test-dir build -R planar_ring --output-on-failure</automated>
  </verify>
  <done>All planar_ring tests pass; `rg -n '% capacityFrames_' Shared/src/PlanarRingBuffer.cpp` returns 0 matches.</done>
</task>

<task type="auto">
  <name>Task 2: Fill metrics helpers for drift loop</name>
  <files>Shared/include/apm44/PlanarRingBuffer.h, Shared/src/PlanarRingBuffer.cpp, tests/test_planar_ring_buffer.cpp</files>
  <action>
Add RT-safe read-only metrics (no allocation):

- `std::size_t fillFrames() const` — alias of `availableToRead()` or explicit name per CONTEXT.

- `double fillMs(double sampleRate) const` — `fillFrames() * 1000.0 / sampleRate`.

- `static std::size_t framesForMilliseconds(double ms, double sampleRate)` — helper for BridgeEngine `prepare()` sizing (default target 15 ms per CONTEXT).

Add test: at 44100 Hz, 662 frames ≈ 15 ms within 0.5 ms tolerance.

Document in header comment: ring tracks **44.1-side** fill (input rate domain).

**Per ENG-03:** configurable target fill is applied in BridgeEngine (plan 05); this plan only provides measurement primitives.
  </action>
  <verify>
    <automated>ctest --test-dir build -R planar_ring --output-on-failure</automated>
  </verify>
  <done>`fillMs(44100)` matches expected ms for known fill; helpers are inline/no heap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RT IOProc | Ring push/pop on audio thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | PlanarRingBuffer | mitigate | Capacity upper bound in `prepare()` caller (plan 05); reject zero capacity |
| T-02-04 | Tampering | Atomic indices | accept | Single-process daemon; no cross-process ring yet |
</threat_model>

<verification>
- Ring tests green; no modulo indexing left in implementation
</verification>

<success_criteria>
- Power-of-two SPSC ring with fill frame/ms reporting ready for drift controller input
</success_criteria>

<output>
After completion, create `.planning/phases/02-production-src-drift-engine/02-02-SUMMARY.md`
</output>
